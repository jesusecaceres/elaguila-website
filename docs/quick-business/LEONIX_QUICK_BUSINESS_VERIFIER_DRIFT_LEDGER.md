# LEONIX QUICK BUSINESS — VERIFIER DRIFT LEDGER (integration + forensic certification gate, 2026-09-20)

Purpose: classify, one by one, every repository verifier that fails or cannot run on the Quick Business feature branch, and decide whether the failure is a stale expectation, a current-main defect, an environment limit, or a Quick regression.

Method used in THIS mission (not carried over from prior docs):
1. Every script below was re-run on branch HEAD `f55ea363ac69e4901bbef47638ad7f7e9ae8ed59` (`npx tsx` for `.ts`, `node` for `.mjs`); the first failing assertion was captured (logs kept in the mission scratchpad).
2. Every script was ALSO run on a throwaway `git worktree` checked out at `origin/main` `fd9094994aa2a63fdcea49f24b2435300a7b49a4` (node_modules symlinked, worktree removed afterwards). The BASE / MAIN RESULT column is that run.
3. Every `app/…` / `scripts/…` path literal each script reads was extracted (`grep -o '"app/[^"]*"'`) and intersected with `git diff --name-only origin/main HEAD` (66 files: Quick Classifieds stack, Quick Business tree, launchpad, gateway, docs, verifiers, package.json). For every script the intersection is EMPTY, i.e. the script reads the same source on the branch as on `origin/main`.
4. "Quick dependency" is answered from the Quick Business source: what Quick reads at runtime is the canonical draft stores, gates, mappings, previews and package keys cited in `LEONIX_QUICK_BUSINESS_FINAL_PROOF_MATRIX.md` — not the copy strings, hub CTAs, legacy constants or promo wiring these tests assert.

Allowed classification: `STALE_VERIFIER` · `CURRENT_MAIN_DEFECT` · `ENVIRONMENT_LIMITATION` · `FEATURE_REGRESSION`. Allowed action: `NO_ACTION_QUICK_UNAFFECTED` · `SEPARATE_MAIN_CLEANUP_REQUIRED` · `QUICK_REPAIR_REQUIRED`.

| # | VERIFIER | CATEGORY | FEATURE BRANCH RESULT (`f55ea363`) | BASE / MAIN RESULT (`fd909499`) | FILES IT READS (∩ branch diff = ∅) | WHY IT FAILS | CLASSIFICATION | QUICK DEPENDENCY | ACTION |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `scripts/autos-a5-1-negocios-draft-preview-persistence-audit.ts` | Autos Dealer | FAIL — `Must save before preview navigation` | FAIL — identical | `AutosNegociosPreviewClient.tsx`, `AutosNegociosApplication.tsx`, `useAutoDealerDraft.ts`, `autosDealerInventoryAddFlow.ts`, an A5.1 audit markdown under `app/lib/clasificados/autos/` | Asserts an older "save before preview" literal in the Full dealer application; the Full app now flushes through `flushDraft({ editorStep })` (`AutosNegociosApplication.tsx:864-870`) and the audit's string expectation was never updated | STALE_VERIFIER | Quick Dealer writes the SAME store the Full flush writes (`saveAutosNegociosDraftResolved` + namespace hint, Proof Matrix AD-06/AD-07) and does not use the Full application's save button | NO_ACTION_QUICK_UNAFFECTED |
| 2 | `scripts/autos-final-only-negocios-privado-readiness-audit.ts` | Autos Dealer / Privado | FAIL — `Negocios VIN decode required` | FAIL — identical | `AutoDealerPreviewPage.tsx`, `AutoPrivadoPreviewPage.tsx`, `autosCtaTracking.ts` + category folders | Asserts a VIN-decode literal on the negocios preview page; the current canonical preview keeps VIN optional (`autoDealerListing.ts:130 vin?: string`) and decodes in the application, not the preview | STALE_VERIFIER | Quick Dealer treats VIN as optional exactly like the canonical type (AD-13); no Quick code reads the preview page literal | NO_ACTION_QUICK_UNAFFECTED |
| 3 | `scripts/autos-a5-qa-07-application-persistence-inventory-truth-audit.ts` | Autos Dealer | FAIL — `Missing required copy: Varias URLs de imágenes, una por línea` | FAIL — identical | `autosNegociosCopy.ts`, `autosNegociosDraftStorage.ts`, `AutosNegociosAddInventoryDrawer.tsx`, `AutosNegociosMediaManager.tsx`, `useAutoDealerDraft.ts` | Asserts a copy string the Full media manager no longer renders (multi-URL paste helper) | STALE_VERIFIER | Quick Dealer never renders the Full media manager; media enters through the certified `QuickMediaStep` and lands as `MediaImageEntry` (AD-15) | NO_ACTION_QUICK_UNAFFECTED |
| 4 | `scripts/autos-a5-0-negocios-missing-publish-blockers-audit.ts` | Autos Dealer | OK on the committed tree (FAIL only while the closeout was uncommitted: `changed file outside allowed scope`) | OK | scope lock over `git status` (uncommitted files) + Full autos files | `assertUnrelatedScope` rejects ANY uncommitted file outside an old Autos mission scope; passes on a clean tree | STALE_VERIFIER (scope lock of an old mission) | none | NO_ACTION_QUICK_UNAFFECTED |
| 5 | `scripts/bienes-hydration-proof-01-core.ts` | Bienes negocio / agent | FAIL — `return draft key saved` | FAIL — identical | agente-individual `previewDraft.ts` behaviour (return-key write) | Expects the legacy unscoped return key to be written on a bare `saveAgenteResPreviewDraft`; the current store is instance-scoped (`brAgenteResApplicationStorageKeys`) and writes the return key only with `writeReturn: true` / an existing return key (`previewDraft.ts:536-588`) | STALE_VERIFIER | Quick Bienes calls `persistAgenteResApplicationDraftResolved(state, { applicationInstanceId, writeReturn: true })` — the exact current contract — and verifies the write (BN-07) | NO_ACTION_QUICK_UNAFFECTED |
| 6 | `scripts/bienes-active-application-refresh-persistence-audit.ts` | Bienes negocio / agent | FAIL 17/18 — `No Supabase imports in scoped files` | FAIL — identical | `AgenteIndividualResidencialApplication.tsx`, `brAgenteResDraftMedia.ts`, `previewDraft.ts`, `brNegocioAdditionalInventoryDraft.ts`, `brNegocioChildInventoryFormMapping.ts`, `brNegocioInventoryDraftPersistence.ts`, `BrNegocioChildInventoryFullApplication.tsx` | A scoped Full file gained a Supabase import on main after this audit was written (the other 17 checks pass) | STALE_VERIFIER (expectation of an older file set) | Quick Bienes imports none of the scoped inventory files; it uses `previewDraft.ts` + the schema merge only (BN-06) | NO_ACTION_QUICK_UNAFFECTED |
| 7 | `scripts/gate-i5-3a-br-gateway-fix-selftest.ts` | Bienes (dashboard CTA) | FAIL — `dashboard BR publish CTA must use BR_PUBLICAR_HUB again` | FAIL — identical | `app/(site)/dashboard/page.tsx`, `dashboardMisAnunciosCategories.ts`, BR hub / selector / gateway resolver | Asserts the dashboard's BR publish CTA constant; main's dashboard CTA now targets a different (newer) route and the selftest was not updated | STALE_VERIFIER | Quick Bienes hands off to the agente preview by literal route and links the dashboard only as a manage destination (`/dashboard/mis-anuncios?cat=bienes-raices`, CM-04); it never reads the dashboard's publish CTA | NO_ACTION_QUICK_UNAFFECTED |
| 8 | `scripts/verify-revenue-os-br-inventory-pack-fulfillment-and-global-preview-safety-01.mjs` | Bienes / Revenue OS | FAIL — `max children must be 4` | FAIL — identical | `publishCheckoutCheckpoint.ts`, `dashboardInventory.ts`, `bienesDashboardInventoryAddonCheckout.ts`, `revenue-os/webhook/route.ts`, `RevenueOsPagoResultView.tsx`, `BrNegocioListingInventoryActions.tsx` | Asserts an OLD allowance constant (`BR_INVENTORY_PACK_MAX_CHILDREN` = 4); source on main is 3 (`publishCheckoutCheckpoint.ts:64`). The branch does not change the constant (PY-06) | STALE_VERIFIER | Quick Bienes never selects the inventory pack (one property; `bundleCreatedCount = 0` in the existing preview) and reads no allowance constant (BN-16) | NO_ACTION_QUICK_UNAFFECTED |
| 9 | `scripts/verify-bienes-application-instance-isolation-01.mjs` | Bienes negocio / agent | FAIL — `new application clears memory only` | FAIL — identical | `AgenteIndividualResidencialApplication.tsx`, `previewDraft.ts`, `brAgenteResDraftMedia*.ts`, agente preview client + page, `brNegocioChildInventoryEditorSession.ts` | Asserts a specific "new application clears memory only" implementation detail of the Full application's reset path that main changed | STALE_VERIFIER | Quick Bienes creates a FRESH `applicationInstanceId` per handoff and never calls the Full application's reset path (BN-07) | NO_ACTION_QUICK_UNAFFECTED |
| 10 | `scripts/verify-bienes-autos-dealer-paid-readiness.mjs` | Bienes + Dealer / Revenue OS | FAIL — `Bienes agent must not wire onPromoApply until webhook fulfillment gate ships` | FAIL — identical | agente preview client, servicios / privado previews, `AutosPublishConfirmCore.tsx`, `autos/checkout/route.ts`, `revenueFulfillment.ts`, `revenuePromoRedemptions.ts` | Asserts that the agente preview must NOT wire promo codes; main has since shipped the webhook fulfillment gate and wired `onPromoApply`, so the guard's expectation is inverted / stale | STALE_VERIFIER | Quick Bienes contains no promo / Stripe code (`verify-quick-business-core-01` §5) and only hands off to that preview; checkout behaviour is main's, unchanged by the branch (PY-01) | NO_ACTION_QUICK_UNAFFECTED |
| 11 | `scripts/smoke-servicios-business-presets.ts` | Servicios | FAIL — preset `mensajeria_delivery::mens_paquetes` resolves the generic icon while a keyword rule suggests 🛵 | FAIL — identical | `businessTypePresets.ts` icon resolution (untouched) | A preset keyword/icon rule in the untouched Servicios preset catalogue disagrees with the smoke's expectation | STALE_VERIFIER (icon expectation of an untouched preset table) | Quick Servicios reads `BUSINESS_TYPE_PRESETS` ids + labels only (SV-02); icons are resolved by the canonical renderer | NO_ACTION_QUICK_UNAFFECTED |
| 12 | `scripts/restaurantes-launch-selftest.ts` | Restaurantes | FAIL — `Missing NEXT_PUBLIC_SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY for DB phase` | FAIL — identical | Restaurantes source + a live-DB phase | The container has no Supabase service-role env; the source phases before the DB phase pass | ENVIRONMENT_LIMITATION | Quick Restaurantes uses only the source-level contract (`createEmptyRestauranteDraft`, `auditRestaurantePublishReadiness`, `saveRestauranteDraftToStorageResolved`) proven by the other Restaurantes verifiers (RS-05/RS-06) | NO_ACTION_QUICK_UNAFFECTED |

## Totals

| Metric | Value |
| --- | --- |
| Verifiers re-run in this gate (all categories + guards + Quick verifiers) | 68 |
| Failing / limited on the feature branch | 11 (rows 1–3, 5–12) + 1 scope lock that passes on the committed tree (row 4) |
| FEATURE_REGRESSION | 0 |
| STALE_VERIFIER | 10 (rows 1–11 minus row 12) |
| CURRENT_MAIN_DEFECT | 0 (every failure is an outdated test expectation, none names a customer-visible defect Quick depends on) |
| ENVIRONMENT_LIMITATION | 1 (row 12) |
| QUICK_REPAIR_REQUIRED | 0 |
| SEPARATE_MAIN_CLEANUP_REQUIRED | 0 flagged as blocking; the 10 stale scripts are candidates for a separate housekeeping mission (not this branch's scope: "do not modify stale verifiers just to make them green") |

Not modified in this mission: any of the scripts above. The Quick Business verifiers that DO gate this branch (`verify-quick-business-core-01`, `verify-quick-business-proof-matrix-02`) and the certified Quick Classifieds verifiers (`verify-quick-classifieds-onramp-01`, `-interaction-02`, `-proof-matrix-03`) all pass on `f55ea363`.
