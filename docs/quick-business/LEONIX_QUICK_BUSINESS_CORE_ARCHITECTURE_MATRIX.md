# LEONIX QUICK BUSINESS CORE — ARCHITECTURE MATRIX (cold map of the four existing business products)

> **Proof pointer (integration gate, 2026-09-20):** every status / "unchanged" / "canonical" statement in this document is backed by a row of `LEONIX_QUICK_BUSINESS_FINAL_PROOF_MATRIX.md` (checked by `scripts/verify-quick-business-proof-matrix-02.ts`): §1 → SV/RS/AD/BN chain rows, §2 → PY-01…PY-06, §3-A → SV-02…SV-11, §3-B → RS-02…RS-11, §3-C → AD-02…AD-23, §3-D → BN-02…BN-20, §4 → SQ-01…SQ-13, §5 → AD-19…AD-22 / BN-17…BN-19 / SI-01…SI-05, §6 → SQ-09 + the category field rows. This document is a lead, not evidence.

Branch `claude/quick-business-core-build-2026-09`, started from the certified Quick Classifieds HEAD `7555fb6456dff1797a7ca8d5716f99abdc511cce` (`origin/main` `fd9094994aa2a63fdcea49f24b2435300a7b49a4`, 11 ahead / 0 behind at start). Every cell was read from CURRENT source on 2026-09-20; prior Quick documents were leads only. **Closeout (same day, commit after `426deab168dcb0aeecd2bf0429272b3ca4b52a55`):** the PM decided that Quick MAY include the customer's REAL first vehicle / first property because the canonical Dealer / Agent products require it; §3-C / §3-D below are now Quick contracts, §5 proves the Dealer + Bienes chains and §6 is the four-category customer-experience audit.

Doctrine applied: **less input → same existing canonical business/category data model → same existing preview → same existing payment → same existing public output → same existing Business Hub / admin / lifecycle.** Quick Business is a two-minute front door into the existing paid business products; it is not a second house.

## 1. Category map

| Item | A. SERVICIOS | B. RESTAURANTES | C. AUTOS DEALER | D. BIENES RAÍCES NEGOCIO / AGENT |
| --- | --- | --- | --- | --- |
| ENTRY ROUTE | `/clasificados/servicios` (hub) · checkpoint `/clasificados/publicar/servicios/checkpoint` (`getServiciosCheckpointCard`, `categoryPublishCheckpoints.ts:205-250`) | `/clasificados/restaurantes` (hub) · checkpoint cards `getRestaurantesCheckpointCards` (`categoryPublishCheckpoints.ts:96-155`, CTA `/publicar/restaurantes?product=established_restaurant`) | `/publicar/autos` (`PublicarAutosBranchClient` → `getAutosCheckpointCards`) | `/clasificados/publicar/bienes-raices` hub → `/publicar/bienes-raices` selector (`PublicarBienesRaicesNegocioSelectorClient`: residencial / comercial / terreno) |
| FULL APPLICATION ROUTE | `/publicar/servicios` → `ClasificadosServiciosApplication.tsx` (8 stepped sections, ~4,100 lines; registry `SERVICIOS_ADAPTER.applicationRoute`) | `/publicar/restaurantes` → `RestauranteApplicationClient.tsx` (single page, sections A–…; registry `RESTAURANTES_ADAPTER.applicationRoute`) | `/publicar/autos/negocios` → `AutosNegociosApplication.tsx` (vehicle-first stepper `AutosNegociosVehicleApplicationSteps` + dealer block + inventory drawer) | `/clasificados/publicar/bienes-raices/negocio` (`negocio/page.tsx` renders `AgenteIndividualResidencialApplication.tsx`; the `agente-individual/` folder holds only its application / preview / schema code — steps 01–09: property first, agent card later) |
| AUTH GATE | `app/(site)/publicar/layout.tsx` + `app/(site)/clasificados/publicar/layout.tsx` = `PublishAuthGateLayout` (customer session or server-verified assisted cookie) | same (`/publicar/**`) + `clasificados/restaurantes/preview/layout.tsx` wraps the preview | same (`/publicar/**`) | same (`/clasificados/publicar/**`) |
| OWNER / BUSINESS IDENTITY | `servicios_public_listings.owner_user_id` from bearer (`api/clasificados/servicios/publish/route.ts`); assisted rows stay `owner_user_id = null` with `business_listing_links` custody (`assistedListingCustody.ts`) | `restaurantes_public_listings.owner_user_id` server-verified only ("client-supplied owner_user_id is never trusted", `restaurantes/publish/route.ts:320-351`) | `autos_classifieds_listings.owner_user_id` from bearer (`getAutosPublishUserIdFromRequest`) | `listings.owner_id` from `supabase.auth.getUser()` inside `publishLeonixRealEstateListingCore` |
| CANONICAL DRAFT | `ClasificadosServiciosApplicationState` in sessionStorage `leonix.clasificados.servicios.application.v1` + IndexedDB media namespace `clasificados-servicios-v1` (`clasificadosServiciosStorage.ts`: `saveClasificadosServiciosApplicationResolved`, `loadClasificadosServiciosApplicationResolved`) + one-shot preview-return payload (`clasificadosServiciosPreviewHandoff.ts`) | `RestauranteListingDraft` in sessionStorage `restaurantes-draft` + IndexedDB (`restauranteDraftStorage.ts`: `saveRestauranteDraftToStorageResolved`, `loadRestauranteDraftFromStorageResolved`) | `AutoDealerListing` draft, namespaced sessionStorage + IndexedDB (`autosNegociosDraftStorage.ts`, `useAutoDealerDraft.ts`) | `AgenteIndividualResidencialFormState` (1,281-line schema) via `bienesRaicesPreviewDraft.ts` / dashboard listing-edit workspace (localStorage) |
| CANONICAL STATE TYPE | `clasificadosServiciosApplicationTypes.ts:169` (`businessTypeId`, `businessName`, `city/state/country`, contact fields, `gallery: GalleryItem[]`, `coverUrl`, `aboutText`, `selectedServiceIds`, `customServicesOffered`, `hours: DayHoursRow[]`, confirmations, credentials, coupons, promotions, …) | `restauranteListingApplicationModel.ts` (`RestauranteBusinessIdentity` + `RestauranteOperatingModel` + `RestauranteWeeklyHours` + `RestauranteContactCta` + `RestauranteGalleryMedia` + stacks + coupons); draft = application minus `RestauranteInternalContract` (`restauranteDraftTypes.ts`) | `AutoDealerListing` (`autoDealerListing.ts`: vehicle fields year/make/model/price/mileage/VIN + dealer fields `dealerName/dealerPhone*/dealerLogo/…` + `mediaImages`) | `AgenteIndividualResidencialFormState` (property: `titulo`, `precio`, `ciudad`, media; agent card: `agenteNombre`, `agenteTitulo`, `agenteLicencia`, phones, `agenteAreaServicio`, second agent, broker) |
| CANONICAL VALIDATOR | `evaluateServiciosPublishReadiness` (`serviciosPublishReadiness.ts:79-138`): business type (+custom label for group `other`), name ≥ 2, city ≥ 2, ≥ 1 valid contact (`hasServiciosValidContactData`: phone ≥ 8 digits / WhatsApp / email / website), about ≥ 8 chars, ≥ 1 service, cover or featured gallery image, 3 confirmations. `evaluateServiciosPreviewReadiness` = same minus the confirmations | `auditRestaurantePublishReadiness` (`restauranteListingApplicationModel.ts:694+`): `businessName`, `businessType`, `primaryCuisine`, `cityCanonical`, hero or first gallery image (`draft` mode accepts data URLs; `transport` mode HTTPS only), ≥ 1 contact path (`hasPrimaryContactPath`), hours signal (`hasOperatingSignal`: every day closed-or-timed, or a special-hours note/entry). Publish API re-runs `satisfiesRestauranteMinimumValidPreview` (`restaurantes/publish/route.ts:13-31`) | `getAutosPreviewCompletenessIssues("negocios")` (`autosPreviewCompleteness.ts:59-99`): media, vehicle title (`vehicleTitle` or year+make+model), price or monthly estimate, location, dealer identity (name / office phone / logo) | `gateBienesRaicesNegocioPreview` (`leonixRequiredForPreviewGates.ts:81-95`): advertiserType, publicationType, `titulo`, `precio`, ciudad or dirección, ≥ 1 photo, petsAllowed — i.e. a PROPERTY |
| PREVIEW | `/clasificados/publicar/servicios/preview` → `ClasificadosServiciosPreviewClient.tsx` (loads the session draft `:333`, `previewReadiness` checklist `:888`, `publishReadiness` gates checkout `:376`, assisted save/publish-for-client buttons `:563+`) | `/clasificados/restaurantes/preview` → `RestaurantePreviewClient.tsx` (loads draft, `auditRestaurantePublishReadiness` `:126`, checkout checkpoint with `RESTAURANTES_CHECKPOINT_CONFIRMATIONS` `:146`) | `/clasificados/autos/negocios/preview` → `AutosNegociosPreviewClient.tsx` (draft / canonical modes, `AUTOS_DEALER_CHECKOUT`) | `…/agente-individual/preview` → `AgenteIndividualResidencialPreviewClient.tsx` (pending insert `activationMode: "pending_payment"` `:389`, checkout `:464`) |
| PAYMENT PACKAGE | `servicios_base_monthly` — 39,900 ¢ / month, `monthly_subscription`, "1 profile/listing" (`revenuePricingMatrix.ts:243-250`); `servicios_offers_addon` RETIRED (included) | `restaurantes_base_monthly` — 39,900 ¢ / month (`:201-208`); `restaurantes_offers_addon` RETIRED (included) | `autos_dealer_monthly` — 39,900 ¢ / month, "10 active vehicles" (`:104-111`) + `autos_dealer_inventory_pack_monthly` 12,900 ¢ "+10 vehicles" (`:120-128`) | `br_agent_monthly` — 39,900 ¢ / month (`:137-144`) + `br_inventory_pack_monthly` 9,900 ¢ "+3 properties" (`:153-160`); FSBO `br_fsbo_45d` is Classifieds and untouched |
| CHECKOUT | preview → `saveServiciosPendingBeforeCheckout` (`postServiciosPublishApi` with `activationMode: "pending_payment"`, bearer) → `startRevenueCategoryCheckout({ ...SERVICIOS_BASE_CHECKOUT })` → `POST /api/revenue-os/checkout` (server price via `validateRevenueCheckoutRequest` → `packageDef.priceCents`) → Stripe → `revenueServiciosFulfillment.ts` | preview → `saveRestaurantePendingBeforeCheckout` (`POST /api/clasificados/restaurantes/publish` `activationMode: "pending_payment"`, bearer) → `startRevenueCategoryCheckout({ ...RESTAURANTES_BASE_CHECKOUT })` → same server route → `revenueFulfillment.ts` | preview → `autosDealerRevenueCheckout.ts` → `AUTOS_DEALER_CHECKOUT` → same route → `autos_dealer_activate_listing` | preview → `startRevenueCategoryCheckout` (`br_agent_monthly`, optional inventory pack add-on) → same route |
| DATABASE TABLE(S) | `servicios_public_listings` (+ `business_listing_links` custody for assisted drafts) | `restaurantes_public_listings` (`listing_json` = the draft) | `autos_classifieds_listings` (lane `negocios`, `inventory_role` main / inventory_vehicle, `dealer_inventory_group_id`) | `listings` (category bienes-raices, negocio branch) + child inventory rows |
| PUBLIC ROUTE | `/clasificados/servicios/[slug]` (`getServiciosPublicListingBySlugForDiscovery`) | `/clasificados/restaurantes/[slug]` (`restaurantes_public_listings.listing_json` → `mapRestauranteDraftToShellData`, same mapper as preview) | `/clasificados/autos/vehiculo/[id]` — a VEHICLE page (`AUTOS_NEGOCIOS_ADAPTER.publicRoute`) with dealer chrome; dealer inventory rendered as related vehicles | `/clasificados/anuncio/[id]` — a PROPERTY page (`BIENES_RAICES_NEGOCIO_ADAPTER.publicRoute`) with the agent card |
| PUBLIC RENDERER | `app/(site)/clasificados/servicios/shell/*` + `[slug]/page.tsx` | `app/(site)/clasificados/restaurantes/shell/*` via `mapRestauranteDraftToShell.ts` | `AutoDealerPreviewPage.tsx` (same component for preview and live) | `AgenteIndividualResidencialPreviewPage.tsx` / BR negocio live shell |
| RESULT CARD | `ServiciosListingResultCard.tsx`, `ServiciosProfessionalResultCard.tsx` | `restauranteCardSummary.ts` + results components | `AutosDealerInventoryVehicleCard.tsx` (vehicles) | BR results cards (properties) |
| ADMIN | `/admin/workspace/clasificados/servicios` | `/admin/workspace/clasificados/restaurantes` | `/admin/workspace/clasificados/autos` | `/admin/workspace/clasificados/bienes-raices` |
| BUSINESS HUB INTEGRATION | `/dashboard/servicios` (owner list: publish/preview/pause-resume via `POST /api/clasificados/servicios/manage`) + `/dashboard/business-tools/business/[businessId]` (Business Hub profile) | `/dashboard/restaurantes` (owner list; edit `/publicar/restaurantes?…mode=listing-edit&listingId=…`, coupon edit mode) + Business Hub | `/dashboard/mis-anuncios` (`AutosClassifiedListingManageCard`, inventory sections `AutosDealerInventoryDashboardSection`) | `/dashboard/mis-anuncios` (`LeonixRealEstateListingManageCard`, inventory drawer) |
| MEDIA PIPELINE | draft `data:` URLs (gallery `GalleryItem{id,url,source:"file"}`, `coverUrl`, `logoUrl`) → IndexedDB offload → `resolveServiciosDraftMediaToRemoteUrls` (`serviciosDraftPublishPrepare.ts:159-194`: `POST /api/clasificados/servicios/draft-media-upload` → Vercel Blob HTTPS) inside `saveServiciosPendingBeforeCheckout` | draft `data:image/*` (`heroImage`, `galleryImages[]`) → IndexedDB → `restauranteDraftPublishPrepare.ts` (`POST /api/clasificados/restaurantes/draft-media-upload`) → HTTPS before `POST …/publish` (transport gate) | `resolveAutosDraftPhotosForPublish` → `/api/clasificados/autos/media/draft-photo-upload` | `listing-images` via `publishLeonixRealEstateListingCore` |
| EDIT PATH | `/publicar/servicios?…` listing-edit (dashboard) → same row (`servicios/publish` owner check) | `/publicar/restaurantes?source=dashboard&mode=listing-edit&listingId=…` | dashboard → `/publicar/autos/negocios?…` (`AUTOS_NEGOCIOS_ADAPTER` edit modes: listing-edit / inventory-edit / inventory-addon) | dashboard listing-edit workspace |
| PAUSE / END PATH | `POST /api/clasificados/servicios/manage` `{ action: "pause" | "resume" }` (`dashboard/servicios/page.tsx:420-428`) | dashboard lifecycle (pause / resume) | `unpublish` / `restore` (owner-only) | BR lifecycle mutations (`callBrLifecycleMutation`: pause / resume / discontinue / republish) |
| RENEWAL | monthly subscription — no one-time renewal; billing through Stripe subscription (Revenue OS) | monthly subscription | monthly subscription (+ inventory pack) | monthly subscription (+ inventory pack) |
| INVENTORY / MENU / SPECIAL MODULES | coupons/offers (included), promotions, testimonials, credentials (license / insurance / certifications), payment methods, amenities, special hours, videos | menu link/file, featured dishes, coupons + flyer, amenities, moving-vendor / home-based / catering stacks, trust fields, videos | STRUCTURED VEHICLE INVENTORY (main vehicle + `inventory_vehicle` children, limit `resolveDealerActiveVehicleLimit`, +10 pack) | STRUCTURED PROPERTY INVENTORY (parent property + child properties, +3 pack), second agent, broker block |
| REQUIRED FIELDS (canonical validator) | businessTypeId (+customServiceDescription when "Otro servicio"), businessName, city, ≥ 1 contact, aboutText, ≥ 1 service, cover / featured image, 3 confirmations | businessName, businessType, primaryCuisine, cityCanonical, hero / first gallery image, ≥ 1 contact path, hours signal | vehicle title (year+make+model), price, location, media, dealer identity | advertiserType, publicationType, titulo, precio, ciudad/dirección, ≥ 1 photo, petsAllowed |
| OPTIONAL FIELDS | everything else in the 8 steps (specialties, reasons, quick facts, highlights, languages, social, reviews, hours, special hours, promotions, coupons, credentials, payment methods, amenities, videos, physical address) | shortSummary / longDescription, secondary cuisines, price level, languages, service modes (no longer required for preview; not required by the publish route), address, social, menu, dishes, coupons, stacks, amenities, videos | dealer profile fields, VIN decode, specs, videos, additional inventory | property detail groups, open house, tour, second agent, broker, social, inventory children |
| STRUCTURED SUBSYSTEMS | coupons / promotions / credentials (all optional) | menu + coupons (optional) | vehicle inventory (REQUIRED main vehicle) | property inventory (REQUIRED main property) |
| CURRENT STAFF-ASSISTED SUPPORT | YES — `create-for-client` handoff mints the assisted cookie (`application-context?category=servicios`), `PREFILL_SUPPORTED = {servicios}` (`create-for-client/page.tsx:27`), preview shows assisted actions | UI-gate only (cookie renders the banner; publish route has no assisted branch) | UI-gate only | UI-gate only |
| CURRENT PUBLISH-FOR-CLIENT SUPPORT | YES — `assistedAction: "save_for_client" | "publish_for_client"` handled by `servicios/publish/route.ts:264-273,380-391,597-654` (row stays unclaimed `owner_user_id = null`, custody in `business_listing_links`; proven by `verify-p0-final-assisted-publishing-bridge-01`) | NO (route requires bearer owner; `assistedListingCustody.ts` comment "Restaurantes next") | NO | NO |

## 2. Pricing lock (Gate 2) — server-authoritative source `app/lib/listingPlans/revenuePricingMatrix.ts`

| Category | Canonical package used by Quick | priceCents | billingMode | Server price authority | Quick change |
| --- | --- | --- | --- | --- | --- |
| Servicios | `servicios_base_monthly` (`SERVICIOS_BASE_CHECKOUT`, `revenueCategoryCheckoutPayload.ts:84-88`) | 39900 | monthly_subscription | `validateRevenueCheckoutRequest` → `packageDef.priceCents` (`revenueCheckout.ts:130-142,352`); client sends no amount | NONE |
| Restaurantes | `restaurantes_base_monthly` (`RESTAURANTES_BASE_CHECKOUT`, `:70-74`) | 39900 | monthly_subscription | same | NONE |
| Autos Dealer | `autos_dealer_monthly` (`AUTOS_DEALER_CHECKOUT`, `:49-52`) + optional `autos_dealer_inventory_pack_monthly` | 39900 (+12900) | monthly_subscription | same | NONE (Quick Dealer feeds the same `AUTOS_DEALER_CHECKOUT` through the existing preview; the inventory pack stays an existing preview add-on) |
| Bienes Raíces Negocio / Agent | `br_agent_monthly` (`:56-59`) + optional `br_inventory_pack_monthly` | 39900 (+9900) | monthly_subscription | same | NONE (Quick Bienes feeds the same `BIENES_RAICES_NEGOCIO_CHECKOUT` through the existing preview; `br_inventory_pack_monthly` only when the existing preview bundles children) |

No Quick Business SKU, no lower Quick price, no `$99` product, no Stripe change. Prices are displayed in Quick only through `getRevenuePackagePriceCents` at render time (never hard-coded).

**FUTURE OWNER COMMERCIAL DECISION (recorded, not implemented):** whether a lower-priced "Quick Business" tier should exist for micro-businesses. Today Quick Business routes every category to the existing $399/month base package.

## 3. Minimum truthful Quick intake (Gate 3) — field classification and final Quick contract

Field classes: `CANONICAL_REQUIRED` (the existing validator refuses without it) · `QUICK_ESSENTIAL` (asked in Quick because the public product is useless without it) · `FULL_ONLY_OPTIONAL` · `STRUCTURED_FOLLOWUP` · `INVENTORY_ONLY` · `BUSINESS_HUB_ONLY`.

### A. SERVICIOS — Quick contract (adapter `serviciosQuickBusinessAdapter.ts`)

| Quick field (visible) | Class | Canonical destination (`ClasificadosServiciosApplicationState`) |
| --- | --- | --- |
| Tipo de servicio (select, 77 existing presets from `BUSINESS_TYPE_PRESETS`) | CANONICAL_REQUIRED | `businessTypeId` |
| Describe tu servicio (text, shown only for "Otro servicio") | CANONICAL_REQUIRED when preset group = other | `customServiceDescription` |
| Nombre del negocio (text) | CANONICAL_REQUIRED | `businessName` |
| Servicios que ofreces (chips from the chosen preset's `suggestedServices`, namespaced ids) | CANONICAL_REQUIRED (≥ 1 with the next row) | `selectedServiceIds` |
| Otros servicios (text, comma-separated) | CANONICAL_REQUIRED (≥ 1 with the previous row) | `customServicesOffered[]` |
| Sobre tu negocio (textarea, ≥ 8 chars) | CANONICAL_REQUIRED | `aboutText` |
| Ciudad o área (free-text city, same `CityAutocomplete freeText` the Full app uses) | CANONICAL_REQUIRED | `city` (state / country = canonical defaults `LEONIX_LB_DEFAULT_STATE/COUNTRY`) |
| Días que atiendes (chips) + Abres / Cierras (time) | QUICK_ESSENTIAL (the public Contact Hub shows "Hoy …" from `hours`; the Full app's default hours are Mon–Fri 9–18 / Sat 10–14, so Quick asks the real days instead of publishing that default) | `hours: DayHoursRow[]` |
| Teléfono / WhatsApp / Correo / Sitio web (≥ 1) | CANONICAL_REQUIRED | `phone`, `whatsapp`, `email`, `website` (contact enables derived by `syncServiciosContactEnables`) |
| Fotos (≥ 1 real) | CANONICAL_REQUIRED (cover or featured gallery) | `coverUrl` = first image, `gallery[]` = all images (`GalleryItem{ id, url, source: "file" }`), `featuredGalleryIds` inferred by `normalizeClasificadosServiciosApplicationState` |
| 3 confirmations (existing `ListingRulesConfirmationSection subject="servicios"`) | CANONICAL_REQUIRED | `confirmListingAccurate`, `confirmPhotosRepresentBusiness`, `confirmCommunityRules` |
| NOT asked (FULL_ONLY_OPTIONAL / STRUCTURED_FOLLOWUP / BUSINESS_HUB_ONLY) | — | specialties, reasons, quick facts, highlights, languages (canonical default `["lang_es"]` kept — the Full app starts with the same value), social, reviews, special hours, promotions, coupons, credentials / license / insurance / certifications, payment methods, amenities, videos, physical street address, service-area notes |

Never fabricated: credentials, certifications, hours (asked), payment methods, service areas, specialties, years in business, languages beyond the canonical default.

### B. RESTAURANTES — Quick contract (adapter `restaurantesQuickBusinessAdapter.ts`)

| Quick field (visible) | Class | Canonical destination (`RestauranteListingDraft`) |
| --- | --- | --- |
| Nombre del restaurante (text) | CANONICAL_REQUIRED | `businessName` |
| Tipo de negocio (select, `RESTAURANTE_BUSINESS_TYPES`) + "Describe" when `other` | CANONICAL_REQUIRED | `businessType`, `businessTypeCustom` |
| Cocina principal (select, `RESTAURANTE_CUISINES`) + "Describe" when `other` | CANONICAL_REQUIRED | `primaryCuisine`, `primaryCuisineCustom` |
| Descripción corta (textarea, optional) | QUICK_ESSENTIAL-optional | `shortSummary` |
| ¿Cómo atiendes? (chips, optional, `RESTAURANTE_SERVICE_MODES`) | FULL_ONLY_OPTIONAL surfaced as optional chips (not required by preview or publish route) | `serviceModes` (+ `dineIn/takeout/delivery/cateringAvailable/pickupAvailable` booleans) |
| Ciudad (free-text city, same control as the Full app) | CANONICAL_REQUIRED | `cityCanonical` |
| Días abiertos (chips) + Abres / Cierras (time) | CANONICAL_REQUIRED (hours signal) — asked truthfully instead of relying on the all-closed default | `monday…sunday: { closed, openTime, closeTime }` |
| Teléfono / WhatsApp / Correo / Sitio web (≥ 1) | CANONICAL_REQUIRED | `phoneNumber`, `whatsAppNumber`, `email`, `websiteUrl` |
| Fotos (≥ 1 real) | CANONICAL_REQUIRED | `heroImage` = first image, `galleryImages[]` = the rest |
| Confirmations | none in Quick — the existing preview checkout carries `RESTAURANTES_CHECKPOINT_CONFIRMATIONS`; the Full application's confirmation checkboxes are local UI state, not draft fields | — |
| NOT asked | — | menu link/file, featured dishes, coupons/flyer, secondary cuisines, price level, languages, address, social, reviews, stacks, amenities, videos, special hours (`productType` set to the canonical default `established_restaurant`, exactly what the Full app assigns when no `?product=` is present) |

Never fabricated: menu, coupons, cuisine (customer selects), hours (asked), delivery options (customer selects or none).

### C. AUTOS DEALER — Quick contract (adapter `autosDealerQuickBusinessAdapter.ts`) — "Tu negocio + tu primer vehículo"

Cold-read truth (unchanged): the canonical Dealer product has no dealership-profile-only publication — the dealer's main row IS a vehicle listing (`inventory_role = main`, `AUTOS_NEGOCIOS_ADAPTER.publicRoute = /clasificados/autos/vehiculo/{id}`), `getAutosPreviewCompletenessIssues("negocios")` (`autosPreviewCompleteness.ts:59-99`) requires media, a vehicle title (`vehicleTitle` or year + make + model), a price / monthly estimate, a location (dealer structured address OR vehicle city/state/ZIP via `listingHasAutosNegociosPreviewLocation`) and a dealer identity (name / office phone / logo), and the API derives the row title from the vehicle (`autosClassifiedsListingService.ts:426-427`). **PM decision:** Quick therefore asks for the dealer identity PLUS the customer's FIRST REAL vehicle. Field classes for this category: `DEALER_IDENTITY_REQUIRED` · `FIRST_VEHICLE_REQUIRED` · `QUICK_USEFUL` (optional, customer-supplied, never defaulted) · `FULL_ONLY` · `INVENTORY_FOLLOWUP`.

| Quick field (visible) | Class | Canonical destination (`AutoDealerListing`, lane `negocios`) |
| --- | --- | --- |
| STEP 1 · Nombre del negocio (text) | DEALER_IDENTITY_REQUIRED (`dealerIdentity` completeness key) | `dealerName` |
| STEP 1 · Ciudad del negocio (free-text city, same `cityField("free")` the certified Autos privado Quick uses) | DEALER_IDENTITY_REQUIRED (`location` completeness key) | `dealerAddressCity` AND vehicle `city` (the vehicle sits at the dealership) |
| STEP 1 · Código postal del negocio (zip) | DEALER_IDENTITY_REQUIRED (`location`) | `dealerAddressZip` AND vehicle `zip`; `dealerAddressState` / `dealerAddress` derived by the canonical `syncDealerAddressFromStructured` (`autosDealerStructuredAddress.ts:106-118`); vehicle `state` / `country` = the same visible defaults the Full vehicle step selects display (`AutosNegociosVehicleApplicationSteps.tsx:308` → `AUTOS_DEFAULT_STATE`), identical to the certified privado Quick adapter |
| STEP 1 · Teléfono / WhatsApp / Correo / Sitio web (≥ 1) | DEALER_IDENTITY_REQUIRED (Quick `atLeastOne`; canonical identity is already satisfied by the name) | `dealerPhoneOffice`, `dealerWhatsapp`, `dealerEmail`, `dealerWebsite` |
| STEP 2 · Año / Marca / Modelo (number + text) | FIRST_VEHICLE_REQUIRED (`title` completeness key) | `year`, `make`, `model`; `vehicleTitle` = `buildVehicleTitle(year, make, model, trim)` — the same derivation the Full flush applies (`useAutoDealerDraft.ts:57-61 applyAutoTitle`) |
| STEP 2 · Versión (text, optional) | QUICK_USEFUL | `trim` (undefined unless typed) |
| STEP 2 · Condición (select nuevo / usado / certificado, optional) | QUICK_USEFUL | `condition` (canonical `"new" / "used" / "certified"`; undefined unless chosen) |
| STEP 2 · Millas (number, optional) | QUICK_USEFUL | `mileage` (undefined unless typed — never fabricated) |
| STEP 2 · Precio (USD) | FIRST_VEHICLE_REQUIRED (`price` completeness key) | `price` (whole dollars; `monthlyEstimate` never set) |
| STEP 2 · VIN (text, optional, 17 chars) | QUICK_USEFUL — VIN is optional in the canonical type (`vin?: string`, `autoDealerListing.ts:130`) and the Full application decodes it later | `vin` (undefined unless typed) |
| STEP 2 · Descripción (textarea, optional) | QUICK_USEFUL | `description` |
| Fotos del vehículo (≥ 1 real) — labeled "foto real del vehículo", never "foto del negocio" | FIRST_VEHICLE_REQUIRED (`media` completeness key) | `mediaImages: MediaImageEntry[]` (`{ id, url: dataUrl, sourceType: "file", isPrimary: i === 0, sortOrder: i }` — the existing vehicle media shape) + `heroImages` (same order); the existing preview's `resolveAutosDraftPhotosForPublish` uploads them exactly as for the Full application |
| Confirmations | none in Quick — `AutosApplicationFinalActions` gates preview on completeness only; publish confirmations live on the existing confirm / checkout surfaces | — |
| NOT asked (FULL_ONLY / INVENTORY_FOLLOWUP) | — | dealer logo, structured street address, dealer hours / special hours, socials, reviews, custom links, languages, finance contact, VIN decode specs, colors / body / drivetrain / transmission / fuel, badges, features, videos, stock number, monthly estimate, **additional inventory vehicles / inventory pack (`additionalInventoryVehicles: []`, `inventoryBoostSelected` never set)** |

Never fabricated: mileage, VIN, price, condition, address, hours, logo, inventory. Store: `saveAutosNegociosDraftResolved(ns, { v: 1, vehicleTitleOverride: false, listing, editorStep: AUTOS_PUBLISH_FINAL_STEP_INDEX, editorMaxReached: AUTOS_PUBLISH_FINAL_STEP_INDEX, additionalInventoryVehicles: [] })` after `rememberAutosDraftNamespaceHint("negocios", ns)` — the same namespace + hint the Full flush writes (`useAutoDealerDraft.ts:388-428`), read back by the existing preview through `loadAutosNegociosCanonicalActiveDraft` (`autosNegociosCanonicalDraftLoad.ts:15-28`). Handoff: `withLangParam("/clasificados/autos/negocios/preview", routeLang)`.

### D. BIENES RAÍCES NEGOCIO / AGENT — Quick contract (adapter `bienesNegocioQuickBusinessAdapter.ts`) — "Tu perfil + tu primera propiedad"

Cold-read truth (unchanged): the canonical agent product has no agent-profile-only publication — the agente-individual application maps into the negocio publish shape with `titulo`, `precio`, `photoUrls` and `publicationType` derived from the property (`mapAgenteResidencialFormStateToNegocioForPublish.ts`), the publisher refuses without photos (`leonixPublishRealEstateFromDraftState.ts:499-507`) and the public route is a property page (`/clasificados/anuncio/{id}`). The Full application opens its preview only after four confirmations (`confirmAll`, `AgenteIndividualResidencialApplication.tsx:636-642`) and keys the preview draft by an `applicationInstanceId` (`previewDraft.ts`: `ensureBrAgenteResApplicationInstanceId`, `persistAgenteResApplicationDraftResolved`, `withBrAgenteResApplicationInstanceParam`). **PM decision:** Quick asks for the professional identity PLUS the customer's FIRST REAL property. Field classes: `AGENT_IDENTITY_REQUIRED` · `FIRST_PROPERTY_REQUIRED` · `QUICK_USEFUL` · `FULL_ONLY` · `PROPERTY_FOLLOWUP`.

| Quick field (visible) | Class | Canonical destination (`AgenteIndividualResidencialFormState` via `mergePartialAgenteIndividualResidencial`) |
| --- | --- | --- |
| STEP 1 · Tu nombre (text) | AGENT_IDENTITY_REQUIRED (the agent card is the business identity; publish `businessName` comes from the mapped identity) | `agenteNombre` |
| STEP 1 · Título (text, optional) | QUICK_USEFUL | `agenteTitulo` (mapping falls back to the canonical role label only when empty — same as Full) |
| STEP 1 · Licencia DRE (text, optional) | QUICK_USEFUL — never fabricated | `agenteLicencia` |
| STEP 1 · Brokerage / oficina (text, optional) | QUICK_USEFUL — never fabricated | `marcaNombre` |
| STEP 1 · Teléfono / WhatsApp / Correo / Sitio web (≥ 1) | AGENT_IDENTITY_REQUIRED (Quick `atLeastOne`) | `agenteTelefonoPersonal`, `agenteWhatsapp`, `correoPrincipal`, `agenteSitioWeb` |
| STEP 2 · Tipo de propiedad (chips residencial / comercial / terreno_lote) | FIRST_PROPERTY_REQUIRED (`publicationType` derives from it) | `categoriaPropiedad` (`sellerTipo: "agente_individual"` = canonical constant) |
| STEP 2 · Tipo de vivienda / comercial / terreno (select, required for the chosen category) | FIRST_PROPERTY_REQUIRED — asked because `formatTipoPropiedadLine` renders the canonical default code (`casa` / `oficina` / `lote_residencial`) on the preview, which would otherwise be a fabricated type | `tipoPropiedadCodigo` / `comercialTipoCodigo` / `terrenoTipoCodigo` (canonical normalizers) |
| STEP 2 · Recámaras / Baños (number, residencial only, optional) | QUICK_USEFUL — never fabricated | `recamaras`, `banos` |
| STEP 2 · Título del anuncio (text) | FIRST_PROPERTY_REQUIRED (`gateBienesRaicesNegocioPreview`: `titulo`) | `titulo` |
| STEP 2 · Precio (USD) | FIRST_PROPERTY_REQUIRED (`precio`) | `precio` (digits string, same as the Full form) |
| STEP 2 · Condición de la propiedad (select excelente / buena / regular / necesita_reparacion) | FIRST_PROPERTY_REQUIRED — asked because the preview always renders "Condición" from `condicion` (`mapBienesRaicesNegocioStateToPreviewVm.ts:295,339,390`) and the canonical factory default is `buena`; Quick refuses to publish a default condition | `condicionPropiedad` |
| STEP 2 · Descripción (textarea, optional) | QUICK_USEFUL | `descripcionPrincipal` |
| STEP 2 · Ciudad (free-text city) | FIRST_PROPERTY_REQUIRED (`ciudad` or `direccion`) | `ciudad` (canonical `resolveBrListingCity` runs in the mapping) |
| STEP 2 · Zona / colonia / referencia (text, optional) · Código postal (optional) | QUICK_USEFUL — no exact address asked, `mostrarDireccionExacta` stays false | `areaCiudad`, `direccionCodigoPostal` |
| Fotos de la propiedad (≥ 1 real, cap 40 = `leonixRealEstateMediaCountError(…, 40)`) — labeled "foto real de la propiedad", never "foto de tu oficina / negocio" | FIRST_PROPERTY_REQUIRED (`≥ 1 photo`) | `fotosDataUrls` + `fotoPortadaIndex: 0` (the existing property media shape; the canonical store offloads to IndexedDB namespace `br-agente-res-v1:<instance>`; `listing-images` upload happens in the existing publisher) |
| 4 confirmations (existing `ListingRulesConfirmationSection subject="property"` + the existing `brAgenteApplicationPricingCopy().confirmPayment` acknowledgement) | AGENT_IDENTITY_REQUIRED (Full `confirmAll`) | `confirmListingAccurate`, `confirmPhotosRepresentItem`, `confirmCommunityRules`, `confirmPaymentAfterPreview` |
| NOT asked (FULL_ONLY / PROPERTY_FOLLOWUP) | — | agent photo, brokerage logo / license / website, second agent, broker / asesor block, socials, reviews, business links, service area, languages, sqft / lot / parking / year built / levels, HOA / community rules / pet rules (`petsAllowed` is fixed to `"no"` by the canonical mapping for every agente listing — canonical behavior, not a Quick choice), highlights / amenities, open house, tour / brochure / listing URL, videos, exact street address, listing status (`estadoAnuncio` canonical default `disponible` → `en_venta`, i.e. "for sale", which is what a sale listing is), **additional inventory properties / inventory pack (`additionalInventoryProperties` untouched = `[]`, `inventoryPackAccepted` never set)** |

Never fabricated: license, brokerage, beds, baths, sqft, address, amenities, condition (asked), status beyond the canonical "for sale" default. Gate: `gateBienesRaicesNegocioPreview(mapAgenteResidencialFormStateToNegocioForPublish(state))` — the category's own required-for-preview gate run on the same canonical mapping the preview publishes through (the Full application currently gates on `confirmAll` + the publisher's photo check; Quick enforces both plus this gate). Store: `createBrAgenteResApplicationInstanceId()` → `persistAgenteResApplicationDraftResolved(state, { applicationInstanceId, writeReturn: true })` → verified by `readAgenteResPreviewDraftRawForApplication({ applicationInstanceId })`. Handoff: `withBrAgenteResLangParam(withBrAgenteResApplicationInstanceParam("/clasificados/publicar/bienes-raices/negocio/agente-individual/preview", id), lang)` — the existing preview resolves that id from the query (`AgenteIndividualResidencialPreviewClient.tsx:112-115,239-241`).

## 4. Quick Business shared foundation (Gate 4) — what is reused vs. added

Reused from the certified Quick Classifieds layer WITHOUT modification: `QuickFieldRenderer`, `QuickMediaStep`, `QuickShell` (`app/(site)/publicar/rapido/_components`), the pure validation (`validateQuickStep`, `validateQuickMedia`, `quickStr`, `quickList`, `quickWholeDollars`), the framework types (`QuickIntakeStep`, `QuickMediaItem`, `QuickConfirmations`, `QuickIntakeValues`) and the shared ES/EN copy (`quickCopy`). Added (thin, additive): `app/lib/quickBusiness/{quickBusinessTypes,quickBusinessRegistry,quickBusinessRoutes,quickBusinessCopy}.ts`, routes `app/(site)/publicar/negocio-rapido/{page,[category]/page}.tsx`, components `_components/{QuickBusinessChooser,QuickBusinessIntakeClient,QuickBusinessReviewStep}.tsx` + `quickBusinessDraftStore.ts` (same heavy-media IndexedDB helper), adapters `_adapters/{serviciosQuickBusinessAdapter,restaurantesQuickBusinessAdapter,autosDealerQuickBusinessAdapter,bienesNegocioQuickBusinessAdapter,index}.ts`, a "Negocios Rápidos" section in the existing staff launchpad, one additive link on the `/publicar` gateway, and `scripts/verify-quick-business-core-01.ts`. Closeout additions (thin): `QuickBusinessDefinition.mediaIntro` (truthful per-category media wording: business / vehicle / property photo), `QuickBusinessConfirmationSurface` `property_agent` (renders the EXISTING `ListingRulesConfirmationSection subject="property"` plus the EXISTING Bienes agente payment acknowledgement copy), `QuickBusinessConfirmations = QuickConfirmations & { paymentAfterPreview }` (draft store + review), and a "Aplicación completa / Full application" link on every launchpad business card.

## 5. Dealer + Bienes closeout — payment / inventory / lifecycle chain proof (source)

| Chain link | AUTOS DEALER (Quick → existing) | BIENES NEGOCIO / AGENT (Quick → existing) |
| --- | --- | --- |
| Quick writes | `AutosNegociosDraftV1` in the user-scoped namespace (`resolveAutosNegociosDraftNamespace`, `autosNegociosDraftNamespace.ts:50-66`) + hint `lx-autos-draft-ns-hint-negocios` | `AgenteIndividualResidencialFormState` under `br-negocio-agente-residencial-preview-draft:<applicationInstanceId>` (+ return key, localStorage fallback, IDB media namespace) |
| Existing preview reads | `loadAutosNegociosCanonicalActiveDraft()` (hint first, then resolved namespace) → mode `draft` (`AutosNegociosPreviewClient.tsx:311-333`) | `loadAgenteResPreviewDraftResolved({ applicationInstanceId })` (`AgenteIndividualResidencialPreviewClient.tsx:239-241`) |
| Pending row | `ensurePendingDealerListing` → bearer session → `resolveAutosDraftPhotosForPublish` (uploads `sourceType: "file"` images) → `POST /api/clasificados/autos/listings` `{ lane: "negocios" }` (`:462-580`); owner = bearer user (`getAutosPublishUserIdFromRequest`), status `draft` → `pending_payment` | `publishLeonixListingFromAgenteResidencialDraft(st, lang, { mode: "main" }, { activationMode: "pending_payment" })` (`:388-390`) → `publishLeonixRealEstateListingCore` (owner from `auth.getUser()`, `listing-images` upload) |
| Checkout | `startRevenueCategoryCheckout({ ...AUTOS_DEALER_CHECKOUT, listingId, addOns: autosDealerSelectedAddOns(totalVehicleCount) })` (`:667-677`) → `autos_dealer_monthly` (server price `validateRevenueCheckoutRequest`); with ONE vehicle `totalVehicleCount = countApplicationInventoryVehicles(0)` → no inventory-pack add-on (`autosDealerInventoryAddOnSelected(total) = total > AUTOS_DEALER_BASE_INCLUDED_VEHICLES`) | `startRevenueCategoryCheckout({ ...BIENES_RAICES_NEGOCIO_CHECKOUT, listingId, … })` (`:464-473`) → `br_agent_monthly`; inventory-pack add-on only when `bundleCreatedCount > 0` (Quick bundles nothing) |
| Activation / row shape | webhook / bypass → `activateAutosDealerListingAtomic` (RPC `autos_dealer_activate_listing`, capacity + lifecycle aware) → `ensureNegociosInventoryGroupingOnActivate` → `promoteNegociosMainInventoryListing`: **the first vehicle becomes the canonical MAIN row** (`inventory_role = "main"`, `dealer_inventory_group_id = row.id`) (`autosClassifiedsListingService.ts:610-660`) | webhook fulfillment → existing BR lifecycle (`activate_pending`); **the first property is the parent (main) property row** counted under the existing included allowance (`BR_BASE_INCLUDED_PROPERTIES`, `BR_TOTAL_ACTIVE_PROPERTY_LIMIT`, `publishCheckoutCheckpoint.ts:60-66`) |
| Inventory allowance | unchanged: `STANDARD_DEALER_ACTIVE_VEHICLE_LIMIT` + the existing `autos_dealer_inventory_pack_monthly` (`autosDealerInventoryPolicy.ts`, `publishCheckoutCheckpoint.ts:72-80`); more vehicles are added from `/dashboard/mis-anuncios?cat=autos` through the existing inventory drawer / add flow | unchanged: included allowance + the existing `br_inventory_pack_monthly`; more properties are added from `/dashboard/mis-anuncios?cat=bienes-raices` through the existing inventory drawer |
| Lifecycle / manage / admin / public | unchanged: unpublish / restore (owner-only), `/clasificados/autos/vehiculo/[id]`, `/admin/workspace/clasificados/autos`, dashboard edit → `/publicar/autos/negocios?…listing-edit` | unchanged: pause / resume / discontinue / republish (`callBrLifecycleMutation`), `/clasificados/anuncio/[id]`, `/admin/workspace/clasificados/bienes-raices`, dashboard listing-edit workspace |
| Quick touches | none of the above — no API call, no upload, no owner id, no price, no Stripe (verifier §5 / §6) | same |

## 6. Customer experience audit — all four Quick Business intakes (source count, default path)

| Category | Screens | Questions asked (visible on the default path) | Required (incl. ≥ 1 contact rule) | Photos | Confirmations | Handoff |
| --- | --- | --- | --- | --- | --- | --- |
| Servicios | business · where · contact · photos · review | 12 (6 business [1 conditional] · city + 3 hours · 4 contact) | 8 (+ ≥ 1 contact, ≥ 1 service) | ≥ 1 business photo | 3 (existing servicios section) | `/clasificados/publicar/servicios/preview` |
| Restaurantes | restaurant · where · contact · photos · review | 13 (7 restaurant [2 conditional] · city + 3 hours · 4 contact) | 7 (+ ≥ 1 contact) | ≥ 1 restaurant photo | none (existing checkout checkpoint) | `/clasificados/restaurantes/preview` |
| Autos Dealer | dealership · first vehicle · photos · review | 16 (7 dealership · 9 vehicle) | 7 (+ ≥ 1 contact) | ≥ 1 VEHICLE photo | none (existing confirm / checkout) | `/clasificados/autos/negocios/preview` |
| Bienes negocio / agent | profile · first property · photos · review | 19 residencial path (8 profile · 11 property); 17 comercial / terreno | 8 (+ ≥ 1 contact) | ≥ 1 PROPERTY photo (≤ 40) | 4 (existing property section + existing agente payment acknowledgement) | `…/negocio/agente-individual/preview?applicationInstanceId=…` |

Classification of every field: see §3 A–D (CANONICAL_REQUIRED / QUICK_ESSENTIAL / DEALER_IDENTITY_REQUIRED / FIRST_VEHICLE_REQUIRED / AGENT_IDENTITY_REQUIRED / FIRST_PROPERTY_REQUIRED / QUICK_USEFUL / FULL_ONLY / INVENTORY_FOLLOWUP / PROPERTY_FOLLOWUP). No Quick field is decorative (verifier §2 wiring detector, self-tested).
