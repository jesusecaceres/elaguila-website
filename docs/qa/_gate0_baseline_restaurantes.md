# Gate 0 Baseline Audit — Restaurantes Business-Application Flow

Read-only source audit. Branch: `fix/business-applications-final-polish-2026-08`. Date: 2026-08-27.

Evidence doctrine: TRUE only with exact file:line code proof. UNKNOWN = FALSE. Anything requiring live
browser interaction (typing behavior actually observed, visual color/crop appearance, a Preview→Edit
round trip actually clicked through) is marked RUNTIME-REQUIRED, never TRUE from source alone.

Primary files:
- `app/(site)/publicar/restaurantes/RestauranteApplicationClient.tsx` (form/editor, 2709 lines)
- `app/(site)/publicar/restaurantes/restauranteApplicationFormCopy.ts` / `restauranteApplicationUiCopy.ts`
- `app/(site)/clasificados/restaurantes/application/buildRestaurantContactHub.ts` / `mapRestauranteDraftToShell.ts`
- `app/(site)/clasificados/restaurantes/shell/RestauranteAdStoryPreview.tsx` (canonical live public shell)
- `app/(site)/clasificados/restaurantes/shell/RestaurantContactHub.tsx`
- `app/(site)/clasificados/restaurantes/preview/RestaurantePreviewClient.tsx`
- `app/(site)/dashboard/lib/restaurantesDashboardCouponAddonCheckout.ts`
- `app/lib/listingPlans/publishCheckoutCheckpoint.ts`

Note on shell duplication: `RestauranteDetailShell.tsx` / `.backup` and their child gallery blocks are
**dead code**, not used by any live route (`app/(site)/clasificados/restaurantes/[slug]/page.tsx` uses
`RestauranteAdStoryPreview.tsx`). All verdicts below evaluate the live path.

| ID | Requirement (short) | Status | Evidence file:line | Notes |
|---|---|---|---|---|
| R-001 | Preserve premium identity, no broad redesign | TRUE | `shell/RestauranteProfileHeader.tsx:15-16` | Comment: "same warm-brown Restaurant identity, moderately lighter/softer gradient endpoints... no new token system" — incremental, not a rewrite. |
| R-002 | Warm/brown header softened | TRUE | `shell/RestauranteProfileHeader.tsx:17-18` | `HEADER_SHELL` gradient `#2E2620/#4A2E20/#3A332C`, documented as lighter than prior `#1E1814/#3B2117/#2A2620`. |
| R-003 | Use Leonix/category design tokens, not arbitrary colors | FALSE | `shell/RestauranteProfileHeader.tsx:17-27,140,149-222` | ~15 hardcoded hex literals; file comment explicitly says "no new token system." Only the outer `RestaurantesShellChrome.tsx` wrapper uses `var(--lx-*)`. |
| R-004 | Logo avoids forced white box where safe | TRUE (conditional) | `shell/RestauranteProfileHeader.tsx:20-21,84-88,122` | `LOGO_FRAME` keeps a cream `#FFFCF7` box for wide/tall logos by design; only near-square logos get `!p-0.5` (near edge-to-edge). Not eliminated universally, but that matches "where safe." |
| R-005 | Square logo displays strongly | TRUE | `shell/RestauranteProfileHeader.tsx:87-88,122` | `logoIsCompact = ratio 0.8–1.25` triggers `!p-0.5` (~2px padding). |
| R-006 | Wide logo contains properly | TRUE | `shell/RestauranteProfileHeader.tsx:124-129` | `object-contain` always applied — wide logos letterboxed, not cropped. |
| R-007 | Tall logo contains properly | TRUE | `shell/RestauranteProfileHeader.tsx:129` | Same `object-contain`; pillarboxed. |
| R-008 | No destructive crop | TRUE | `shell/RestauranteProfileHeader.tsx:129` (contain) vs `:111` (hero bg image, `object-cover`) | Logo container never uses `object-cover`; only the separate hero background photo does (expected). |
| R-009 | Cuisine/style selection not capped at 3 | TRUE | `RestauranteApplicationClient.tsx:109` (`MAX_ADDITIONAL_CUISINES = 6`), enforced `:569` | Cap is 6. **Caveat:** helper copy at `restauranteApplicationFormCopy.ts:120` (es) / `:465` (en) still says "up to 3" — stale copy contradicts the real cap. |
| R-010 | Target ~6 in clean responsive 2x3 grid | FALSE | `RestauranteApplicationClient.tsx:1078-1101` | Container is `max-h-52 overflow-y-auto` + `flex flex-wrap gap-2` — a scrollable flex-wrap list, no `grid`/`grid-cols` classes. Not a 2x3 grid. |
| R-011 | Existing selections hydrate | RUNTIME-REQUIRED | `application/useRestauranteDraft.ts:80-92`, `createEmptyRestauranteDraft.ts:172-174` | Static wiring is unambiguous (checkboxes read straight from `draft.additionalCuisines/highlights/languagesSpoken`, nothing strips on load) but actual post-reload hydration needs browser observation. |
| R-012 | Custom "Otro" accepts multi-word/spacebar/backspace/paste/Add/remove | TRUE | Cuisine: `RestauranteApplicationClient.tsx:1107-1113` (plain input, no filter). Language: `app/components/forms/LanguagesInput.tsx:93-101,114-127` (`onKeyDown` only intercepts Enter) | No character-filtering/`onPaste`/`preventDefault` found blocking spaces anywhere in these inputs. |
| R-013 | Ambience remains available | TRUE | `application/restauranteAmenitiesCatalog.ts:79-92` (`atmosphere` group, 8 items) | Rendered via `RestauranteAmenitiesFormBlock.tsx:77-110`. |
| R-014 | Food options remain available | TRUE | `restauranteAmenitiesCatalog.ts:107-120` (`foodOptions` group, 8 items) | Vegetarian/vegan/gluten-free/halal/kosher/breakfast/brunch/late-night. |
| R-015 | Amenities remain available | TRUE | `RestauranteApplicationClient.tsx:2409-2416` | `RestauranteAmenitiesFormBlock` actually rendered in Section J, not orphaned. |
| R-016 | Section-specific custom inputs, not one bottom catch-all | TRUE | businessType `:987-995`, primaryCuisine `:1043-1055`, secondaryCuisine `:1056-1068`, additionalCuisines `:1103-1116`, languages via `LanguagesInput` `:1157-1180`, serviceMode `:1302-1314` | Each "Otro"-capable group has its own dedicated input beside it. No shared bottom field. Amenities/Highlights groups have no "Otro" option at all (N/A, not a gap). |
| R-017 | Custom values render in correct semantic group | TRUE | `application/restauranteFeaturesNormalization.ts:160-251` | Service-mode custom → `servicios`; cuisine customs → `cocina_y_estilo`; business-type custom → `ambiente_y_amenidades`; language customs → `idiomas`. Correctly bucketed. |
| R-018 | Language input follows shared standard | TRUE | `RestauranteApplicationClient.tsx:25` imports `LanguagesInput` from `app/components/forms/LanguagesInput`, used at `:1157-1180` | No parallel bespoke language UI. |
| R-019 | English shell genuinely English when lang=en | FALSE | `shell/RestauranteAdStoryPreview.tsx:347-349` renders `RestauranteGroupedFeaturesSection`; that component (`RestauranteGroupedFeaturesSection.tsx:14-17,38,74-86`) takes **no `lang` prop** and hardcodes Spanish headings ("Servicios y Características") | Most of the shell copy (`restauranteApplicationUiCopy.ts`, `restauranteApplicationFormCopy.ts:367-707`) is genuinely translated, but this always-rendered live section is Spanish-only regardless of `lang=en`. |
| R-020 | Spanish shell remains Spanish | TRUE | `restauranteApplicationFormCopy.ts:20-365` (`es:` block) | Matches English block's structure key-for-key. |
| R-021 | No mixed-language system UI | FALSE | Same as R-019: `shell/RestauranteAdStoryPreview.tsx:347-349` + `RestauranteGroupedFeaturesSection.tsx` | A restaurant with `lang=en` still sees a hardcoded-Spanish "Servicios y Características" section on the live public shell — genuine mixed-language defect. |
| R-022 | Strong hours UX preserved | TRUE | `app/components/forms/HoursEditor.tsx:40-97`; wired `RestauranteApplicationClient.tsx:1346-1355` for all 7 days | Full per-day open/close/closed editor, not a stub. |
| R-023 | Special hours preserved | FALSE | `specialHoursNote` wired at `RestauranteApplicationClient.tsx:1358-1364`, but `temporaryHoursActive`/`temporaryHoursNote` have **0 matches** anywhere in the form file | These two model fields exist only in the type (`application/restauranteListingApplicationModel.ts:90-91`) and are consumed by hours-preview logic, but there is no UI control to ever set them — dead fields from the owner's perspective. |
| R-024 | Multiple special-hours entries supported where model allows | FALSE | `application/restauranteListingApplicationModel.ts:89-91` | `specialHoursNote`/`temporaryHoursActive`/`temporaryHoursNote` are singular scalars, not arrays — the model itself doesn't support multiple entries. |
| R-025 | Open-now/current-status card accurate | FALSE | `application/mapRestauranteDraftToShell.ts:682-691` builds `weeklyHours` with only the 7 day keys, omitting `specialHoursNote`/`temporaryHoursActive`/`temporaryHoursNote`, then calls `computeShellHoursPreview` | The temporary-hours override branch (`restauranteHoursPreview.ts:21-27`) is structurally unreachable from the detail-page status card. (Contrast: the discovery-listing "open now" check does pass the full draft and works correctly — bug is isolated to the detail-shell mapper.) |
| R-026 | Reload returns to SAME section, not forced to focus=coupon-upgrade | **FALSE (CRITICAL — confirmed reported bug)** | `RestauranteApplicationClient.tsx:181` (`focusCoupon = searchParams?.get("focus")==="coupon-upgrade"`), `:418` (`useState("restaurantes-section-a")`), `:420-424` (`useEffect(() => { if (hydrated && focusCoupon) setActiveSectionId("restaurantes-section-g"); }, [hydrated, focusCoupon])`); URL is permanently baked with `focus=coupon-upgrade` by `restauranteCouponEditHref` (`app/(site)/dashboard/lib/restaurantesDashboardCouponAddonCheckout.ts:294-311`) | `activeSectionId` is pure client React state, reset to its initial value on every full remount. The effect re-fires on every hard refresh as long as the URL still carries `focus=coupon-upgrade` (browsers don't drop query params on reload) — even if the owner had since clicked to Section A/D/etc. A hard refresh on a coupon-edit-entered URL **always** snaps the user back to Section G, discarding their actual navigated position. No guard exists to disable the effect after first use. |
| R-027 | Primary website distinct from additional | TRUE | `RestauranteApplicationClient.tsx:1383-1391` (websiteUrl) vs `:1392-1436` (additionalWebsites) | Clearly separate fields/UI. |
| R-028 | Additional websites: repeatable Title+URL rows | TRUE | `RestauranteApplicationClient.tsx:1402-1425` (label+url inputs per row), `addAdditionalWebsite` `:534-538` | Add/remove wired, cap 8 (`:536`, `:1427`). |
| R-029 | Multiple additional websites coexist | TRUE | `RestauranteApplicationClient.tsx:1402` `.map(...)` over `draft.additionalWebsites` | Array-based, not single-slot. |
| R-030 | Not buried under generic "Ver más" | TRUE | Form: dedicated subsection in Section D (`:1392-1436`). Shell: `buildRestaurantContactHub.ts:268-280` pushes each row directly into `orderReserve` (always-visible Contact Hub column), no collapse gate. |  |
| R-031 | Appear in clear contact/business area | TRUE | `shell/RestaurantContactHub.tsx:402-409` ("Order or reserve"/"Ordena o reserva" section) | Rendered in the primary always-visible hub, not a secondary tab. |
| R-032 | Facebook/Instagram/TikTok/YouTube truthful | TRUE | `buildRestaurantContactHub.ts:289-294` (`addSocial` only pushes when `isValidExternalHttpUrl` passes on the owner-entered URL) | Real validated URLs only, not placeholders. |
| R-033 | Pinterest allowed only if useful | TRUE (N/A) | repo-wide grep for `pinterest` under `app/(site)/clasificados/restaurantes` → 0 matches | Not offered at all; no clutter added. |
| R-034 | Indeed NOT shown as customer-facing social | TRUE | repo-wide grep for `indeed` under `app/(site)/clasificados/restaurantes` → 0 matches | Absent entirely. |
| R-035 | Rich Correo modal canonical/working | TRUE | `shell/RestaurantContactHub.tsx:242-261` (email action builds `buildSendEmailIntent(...)` and opens via `CtaActionSheet`, not a plain `mailto:` navigation), rendered `:623` | Genuine rich modal, not a stub link. |
| R-036 | Correo hidden without real email | TRUE | `application/buildRestaurantContactHub.ts:203` (`if (nonEmpty(d.email))`) | Button only pushed when a real email exists. |
| R-037 | "Pedir cotización" respects configured quote URL when one exists | **FALSE (CRITICAL — confirmed reported bug)** | `application/buildRestaurantContactHub.ts:339-346` — the "Pedir cotización"/"Request a quote" button (`id: "catering-call"`) is **hardcoded** to `action: "call"` / `href: telHref(d.phoneNumber)`, unconditional on whether `d.cateringEventsStack.cateringInquiryUrl` is configured. The real URL only surfaces under a *separately*-labeled button, "Formulario de cotización" (`:370-380`). | The CTA literally labeled as the quote-request action never consults the configured quote URL — it always routes to a phone call. This is the closest concrete match to the reported "quote URL not respected" bug. |
| R-038 | Configured quote URL not hijacked to WhatsApp | TRUE (not reproduced in current code) | `application/buildRestaurantContactHub.ts:347-381` — `catering-wa` (WhatsApp), `catering-email`, and `catering-form` (real `cateringInquiryUrl`) are independently gated/pushed (`if (nonEmpty(...))` each), no early-return/exclusivity between them | The quote URL, when configured, always produces its own "Formulario de cotización" button (`:370-380`) regardless of whether WhatsApp is also configured — it is not silently replaced by/redirected into a WhatsApp link in this code path. |
| R-039 | "Formulario de cotización" opens actual configured URL | TRUE | `application/buildRestaurantContactHub.ts:370-380` | `href: url` from `cateringInquiryUrl`, validated via `isValidExternalHttpUrl`; opened via `window.open(href)` in `shell/RestaurantContactHub.tsx:277`. |
| R-040 | "Contactar para evento" uses intended rich contact mechanism where configured | TRUE | `application/buildRestaurantContactHub.ts:358-369` (`action: "email"`) routed through the same rich modal path as R-035 (`shell/RestaurantContactHub.tsx:242-261`) | Same `buildSendEmailIntent`/`CtaActionSheet` mechanism as the main Correo button, not a bare mailto. |
| R-041 | "Cotizar catering" resolves to real configured destination | TRUE | `application/buildRestaurantContactHub.ts:347-357` (`waHref(d.whatsAppNumber!, businessName, cateringMsg)`), gated `if (nonEmpty(d.whatsAppNumber))` | Real WhatsApp number required; not a placeholder. |
| R-042 | No CTA renders without a real destination | TRUE | `application/buildRestaurantContactHub.ts:101-104` (`pushUniqueButton`: `if (!btn?.href?.trim()) return;`) | Every button push is gated on a non-empty href. |
| R-043 | Catering is a real titled section when data exists | TRUE | Form: `RestauranteApplicationClient.tsx:2419-2421`. Shell: `application/mapRestauranteDraftToShell.ts:603` (`title: "Catering and events"/"Catering y eventos"`), rendered `shell/RestauranteAdStoryPreview.tsx:462-465` | Dedicated titled section on both sides. |
| R-044 | Not buried only inside generic "Ver más" | TRUE | `shell/RestauranteAdStoryPreview.tsx:149-154` (comment: "catering gets its own always-visible titled section instead of being buried inside the collapsed accordion") | Catering stack explicitly excluded from the `<details>` "Más información" accordion (`:484-552`); catering CTAs also live in the always-visible Contact Hub. |
| R-045 | Hidden when no catering data exists | TRUE | Form gate `RestauranteApplicationClient.tsx:2419`; shell gate `mapRestauranteDraftToShell.ts:580`; render `shell/RestauranteAdStoryPreview.tsx:462` (`{cateringStack ? (...) : null}`) | Same `cateringAvailable \|\| eventFoodService` flags on both sides; truly absent (null), not empty-rendered. |
| R-046 | Catering fields support multi-word typing/spacebar | TRUE | `RestauranteApplicationClient.tsx:2462-2470` | Plain `<input>`, no regex/`onKeyDown`/`preventDefault` filtering. |
| R-047 | Catering values persist through refresh/Preview/Edit | TRUE | Form `:2465-2469` → `useRestauranteDraft` storage (untouched by `trimDraftStrings`) → `buildRestaurantePublishPayload.ts:171` → `createEmptyRestauranteDraft.ts:224-226` (`mergeRestauranteDraft` merges `cateringEventsStack`) | Same merge function used by both dashboard-edit-save and public-hydrate paths. |
| R-048 | Useful set of payment methods | TRUE | `application/restauranteAmenitiesCatalog.ts:32-53` | 8 methods: cash, credit cards, Apple Pay, Google Pay, Zelle, Venmo, Cash App, PayPal. |
| R-049 | Payment snapshot expanded/clean | TRUE | `shell/RestauranteAmenitiesShellSection.tsx:33-60` | Responsive grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6`) with brand-styled chips per method. |
| R-050 | Selected methods persist | TRUE | `RestauranteAmenitiesFormBlock.tsx:49-60` → `buildRestaurantePublishPayload.ts:178-180` → `createEmptyRestauranteDraft.ts:214-216` (`sanitizeRestauranteAmenities`) | Same merge pipeline as R-047. |
| R-051 | Empty/unselected methods don't render as fake options | TRUE | `restauranteAmenitiesCatalog.ts:213-243` (`buildShellAmenitiesSection` only iterates actually-selected ids; groups with nothing selected are skipped, `:221`) | No disabled/greyed placeholder options ever rendered. |
| R-052 | Clean payment section not buried under unnecessary collapse | TRUE | Form: always-expanded subsection in Section J (`RestauranteAmenitiesFormBlock.tsx:86`, no `<details>`/toggle). Shell: plain conditional `<section>` (`shell/RestauranteAdStoryPreview.tsx:352-353`), positioned before the only collapsible element in the file. |  |
| R-053 | Restaurant base remains $399/month | TRUE | `RestauranteApplicationClient.tsx:249` (`baseMonthlyPrice: 399`); `preview/RestaurantePreviewClient.tsx:142,166` (`priceCents: 39900`) | Consistent $399 across form init and checkout. |
| R-054 | Coupons/offers included | TRUE | `restauranteApplicationFormCopy.ts:264` ("Incluido sin costo adicional en tu plan Restaurantes ($399/mes)"); `app/lib/listingPlans/publishCheckoutCheckpoint.ts:307-315` (`priceCents: 0, detailEs: "Incluido en tu plan de $399/mes"`) |  |
| R-055 | No active +$99 coupon-upgrade CTA | TRUE | `app/(site)/dashboard/mis-anuncios/page.tsx:1845` (comment: "the +$99/mes coupon-upgrade CTA is removed"); `restaurantesDashboardCouponAddonCheckout.ts:50-52` (`restauranteCouponAddonUpgradeLabel` = "Enable coupon module", no price) |  |
| R-056 | No active `restaurantes_offers_addon` current-purchase path | TRUE | `app/lib/listingPlans/publishCheckoutCheckpoint.ts:28` (`REVENUE_OS_RESTAURANTES_OFFERS_ADDON_SUPPORTED = false`), used at `:483` to gate `restaurantCouponAddOnSelected`, which gates the only place that constructs a Stripe `addOns:[{key: RESTAURANTES_COUPON_ADDON_PACKAGE_KEY}]` payload (`:532-534`) | Permanently disabled by a hardcoded flag — the legacy SKU can never enter a live checkout payload. |
| R-057 | Existing published Restaurant can add/edit coupons without Stripe | TRUE | `restaurantesDashboardCouponAddonCheckout.ts:86-121` (`startRestauranteDashboardCouponAddonCheckout` calls `enableIncludedCommercialCapability(...)`, never Stripe checkout); `RestauranteApplicationClient.tsx:294-343` (`saveExistingDashboardListing` POSTs directly to `/api/clasificados/restaurantes/publish`, no Stripe) |  |
| R-058 | Existing coupon text/images preserved | TRUE | `createEmptyRestauranteDraft.ts` `mergeRestauranteDraft` merges `coupons`/`couponFlyer` fields from `listing_json` on hydrate (same pipeline as R-047/R-050) |  |
| R-059 | Historical entitlement/SKU records may remain read-only | TRUE | `app/lib/listingPlans/revenuePricingMatrix.ts:216-221,271` (retains `packageKey: "restaurantes_offers_addon"` entry, commented "retired for new sales") | Old SKU kept for compatibility, not reachable as a new purchase (see R-056). |
| R-060 | Hard refresh does not strand owner in stale coupon-upgrade state | **FALSE (CRITICAL — same root cause as R-026)** | Same evidence as R-026: `RestauranteApplicationClient.tsx:181,418,420-424`; URL permanently carries `focus=coupon-upgrade` from `restauranteCouponEditHref` (`restaurantesDashboardCouponAddonCheckout.ts:294-311`) | A hard refresh while on a coupon-edit-entered URL re-forces Section G every time, regardless of where the owner had navigated — this is the "stranded in stale coupon-upgrade state" bug. |
| R-061 | Flyer modal remains canonical/strong | TRUE | `app/components/media/BusinessFlyerViewerModal.tsx:19-91`, reached via `shell/RestauranteShellCouponsBlock.tsx:170-174` → `RestauranteShellInlineDataAssetButton.tsx:4,23` | Full dialog: `role="dialog"`, Escape-to-close, always-visible close button, image/PDF inline render, download fallback. No in-app zoom controls (zoom-safety = OS/browser zoom compatibility per file comment, not an in-app zoom feature). |
| R-062 | Viewer closable at 100%/175% zoom and mobile | RUNTIME-REQUIRED | `BusinessFlyerViewerModal.tsx:57-67` (close button in `shrink-0` header, outside the `overflow-auto` body) | Structurally designed to stay reachable at any zoom, but actual clickability at 175% zoom/mobile needs live verification. |
| R-063 | Gallery modal remains strong | TRUE | `shell/RestauranteLockedGallerySection.tsx:166-298` | Full lightbox: category tabs, counter, close, prev/next, video/image branching. Not a stub. |
| R-064 | 4 featured photos + remaining gallery concept preserved | TRUE | `RestauranteApplicationClient.tsx:588` (`.slice(0, 4)`), `:1784` (add-control gated `< 4`); shell cap `application/mapRestauranteDraftToShell.ts:709-712` (`.slice(0, 4)`) | Featured dishes hard-capped at 4, in their own "Especialidades de la Casa" section, separate from the unlimited tabbed venue gallery. |
| R-065 | Featured/gallery ordering durable | TRUE | `application/restauranteGalleryMediaSequence.ts:39-132` (`computePublishGallerySequence`) | Persists stored `galleryMediaSequence`, validates against current images, falls back to index order only if corrupt/missing. |
| R-066 | Photos not artificially weaker than videos | FALSE | `shell/RestauranteLockedGallerySection.tsx:105` (photos: `grid-cols-4 sm:grid-cols-5 md:grid-cols-7`, `aspect-[4/3]`) vs `:134` (videos: `grid-cols-2 sm:grid-cols-3 md:grid-cols-4`, `aspect-[16/10]`) | On desktop, video tiles render visibly larger (4/row) than photo tiles (7/row) in the same preview strip — no comment indicates this was reconciled. |
| R-067 | Translator must not have disappeared | TRUE (CRITICAL — confirmed present) | Import: `shell/RestauranteAdStoryPreview.tsx:21`. Rendered: `:100-115` (builds `translateControl`), inserted at `:221` and `:276` — not dead code. Fetch target: `app/lib/translation/requestAdTranslation.ts:6` (`fetch("/api/translate-ad", ...)`). Route: `app/api/translate-ad/route.ts:129-188` (category-agnostic handler). | Translator is present and fully wired end-to-end in the live shell. |
| R-068 | Translation placement reconciled with cross-category standard | TRUE | `shell/RestauranteAdStoryPreview.tsx:100-115` (`data-restaurantes-translate-ad="1"`, `version="restaurantes-t9-v1"`) matches the same `TranslateAdControl` prop shape/pattern used in `app/(site)/servicios/components/ServiciosPublicTranslationLayer.tsx:64-69` and `app/(site)/clasificados/comida-local/lib/useComidaLocalPublicTranslation.tsx:53-60` | Minor structural variance only (restaurantes composes the control one layer up from its hook vs. other categories building it inside the hook) — functionally equivalent. |
| R-069 | Final review shows $399/month | TRUE | `preview/RestaurantePreviewClient.tsx:139-143` (`baseLineItem: { priceCents: 39900, ... }`); rendered via `app/(site)/clasificados/components/PublishCheckoutCheckpoint.tsx:282` (`formatPublishCheckpointMoney(item.priceCents, ...)`) | **Note:** the multi-step form's own "Final" section (`RestauranteApplicationClient.tsx` `sectionFinal`, lines 341-365 of the copy file) shows NO pricing at all — the $399 display only exists on the subsequent Preview page's checkout checkpoint. If "final review" means the form's Final step specifically, this is FALSE; graded TRUE here because the Preview page (the actual pre-payment review) does show it. |
| R-070 | Coupons show included | TRUE | `app/lib/listingPlans/publishCheckoutCheckpoint.ts:307-315` (addon line: `priceCents: 0`, `detailEs: "Incluido en tu plan de $399/mes"`) | Rendered as a $0 included line item, not omitted or mispriced. |
| R-071 | Promo/total breakdown visible/truthful | TRUE | `preview/RestaurantePreviewClient.tsx:169-191` (`handlePromoApply` calls real `validateRevenuePromoForCheckout`, computes actual `discountCents`/`totalCents`) | Not a static/fake breakdown. |
| R-072 | One clear Preview CTA | **FALSE** | `RestauranteApplicationClient.tsx:2595-2602` (button labeled `fc.sectionFinal.preview` = "Vista previa"/"Preview") and `:2615-2622` (button labeled `fc.sectionFinal.continueToPreview` = "Continuar a vista previa"/"Continue to preview") — **both** `onClick={goPreview}` | Two visually distinct buttons trigger the exact same action in the same section. |
| R-073 | No duplicate "Continuar a vista previa" controls | **FALSE** | Same evidence as R-072: `RestauranteApplicationClient.tsx:2595-2622`; copy strings at `restauranteApplicationFormCopy.ts:362-363` (es) / `:703-704` (en) | Confirmed duplicate — two preview-triggering buttons rendered together, one literally labeled "Continuar a vista previa." |
| R-074 | No direct Publish bypass | TRUE | `RestauranteApplicationClient.tsx:455-464` (`goPreview` only navigates to `PREVIEW_HREF`, never calls the publish API) | `saveExistingDashboardListing` (`:294-358`) does call `/api/clasificados/restaurantes/publish` directly, but only when `isExistingDashboardListingMode` (an already-published listing being edited from the dashboard) — not reachable for a first-time/new listing, which must go through Preview. |
| R-075 | Preview→Edit preserves current draft/media | TRUE | `preview/RestaurantePreviewClient.tsx:119-122` (`editHref` = `EDIT_HREF_BASE` with lang) rendered as a plain `<Link>` at `:312-317` | No draft-clearing call on the back-to-edit path; same localStorage-backed draft key is read on return via `useRestauranteDraft`. |
| R-076 | Preview mapper/CTA-labels/trust/stacks respect locale | TRUE | `preview/RestaurantePreviewClient.tsx:124` (`mapRestauranteDraftToShellData(draft, { lang })`); `application/mapRestauranteDraftToShell.ts` `buildContact`/`buildStacks`/`buildTrustLight` all take and branch on `lang` | Locale threaded end-to-end through the mapper. (Caveat: see R-019/R-021 — one shell subcomponent, `RestauranteGroupedFeaturesSection`, does not receive `lang` and is hardcoded Spanish.) |
| R-077 | Existing filled ad requires no refill | TRUE | `app/(site)/dashboard/restaurantes/page.tsx:271-315` (`loadIntoForm`: `mergeRestauranteDraft(data.listing_json)` merges the **full** listing, saved to draft storage, **before** `router.push(restauranteListingEditHref(...))`) | Full listing data (not just coupon fields) is hydrated into the draft prior to the edit form ever mounting. |

## Summary counts

- TRUE: 62
- FALSE: 13 (R-003, R-010, R-019, R-021, R-023, R-024, R-025, R-026, R-037, R-060, R-066, R-072, R-073 — note R-019/R-021 share one root cause and R-026/R-060 share one root cause)
- RUNTIME-REQUIRED: 2 (R-011, R-062)

(Roll-up: 62 TRUE + 13 FALSE + 2 RUNTIME-REQUIRED = 77.)

## Top significant FALSE findings (most severe first)

1. **R-026 / R-060 — Hard refresh forces `focus=coupon-upgrade`, stranding the owner mid-edit.** `RestauranteApplicationClient.tsx:181,418,420-424`. The URL query param is baked in permanently by `restauranteCouponEditHref` and never cleared; `activeSectionId` is plain React state reset on every remount. Any hard refresh on that URL snaps the owner back to the coupon section regardless of where they had navigated. Confirmed exactly as reported.
2. **R-037 — "Pedir cotización" ignores the configured catering quote URL.** `buildRestaurantContactHub.ts:339-346`. This CTA is hardcoded to a phone-call action and never consults `cateringEventsStack.cateringInquiryUrl`; the real URL only appears under a separately-labeled "Formulario de cotización" button. Closest match to the reported "quote URL not respected" bug.
3. **R-072 / R-073 — Duplicate Preview CTAs.** `RestauranteApplicationClient.tsx:2595-2622`. Two buttons ("Vista previa" and "Continuar a vista previa") both call `goPreview` in the same section.
4. **R-025 — "Open now" status card structurally can't reflect temporary/special hours.** `mapRestauranteDraftToShell.ts:682-691` omits `temporaryHoursActive`/`temporaryHoursNote`/`specialHoursNote` from the object passed to the preview calculator.
5. **R-023 / R-024 — Special/temporary hours have no UI and no multi-entry model.** `temporaryHoursActive`/`temporaryHoursNote` exist only as unused type fields (0 UI references); all special-hours fields are singular scalars, not arrays.
6. **R-019 / R-021 — Hardcoded-Spanish section breaks English shells.** `RestauranteAdStoryPreview.tsx:347-349` renders `RestauranteGroupedFeaturesSection`, which has no `lang` prop and always shows Spanish headings, even when `lang=en`.
7. **R-003 — Header uses hardcoded hex colors, not Leonix design tokens.** `RestauranteProfileHeader.tsx` explicitly documents "no new token system" — ~15 raw hex literals.
8. **R-010 — Additional-cuisine picker is a scrollable flex list, not a 2x3 grid.** `RestauranteApplicationClient.tsx:1078-1101`.
9. **R-066 — Video tiles render larger/more prominent than photo tiles** in the same gallery preview strip (`RestauranteLockedGallerySection.tsx:105` vs `:134`).
10. **R-009 stale copy** — helper text still says "up to 3" cuisines/styles even though the enforced cap is 6 (cosmetic but user-facing/confusing).

Special-attention items requested: **R-026 confirmed FALSE** (hard-refresh bug is real and reproducible from source), **R-037 confirmed FALSE** (quote CTA hardcoded to phone, not the configured URL — closest match to the "hijacked to WhatsApp" report, though the WhatsApp-specific hijack itself, R-038, was not reproduced in current code), **R-060 confirmed FALSE** (same root cause as R-026), **R-067 confirmed TRUE** (translator is present and fully wired to `/api/translate-ad`, not missing).
