# Gate 0 Baseline Audit — Comida Local

Read-only baseline audit of the Comida Local business-application flow, evaluated against 113
exact requirements (C-001–C-113). Branch: `fix/business-applications-final-polish-2026-08`.
Evidence doctrine: TRUE requires exact file:line proof; UNKNOWN counts as FALSE; anything
requiring live browser interaction is marked RUNTIME-REQUIRED (never TRUE from source alone).

Key files:
- `app/(site)/publicar/comida-local/ComidaLocalApplicationClient.tsx` — main application form
- `app/lib/clasificados/comida-local/comidaLocalConstants.ts` — option registries
- `app/lib/clasificados/comida-local/comidaLocalTypes.ts` — draft type definitions
- `app/lib/clasificados/comida-local/comidaLocalPackages.ts` — legacy $99/$149 tier defs (superseded)
- `app/lib/listingPlans/revenuePricingMatrix.ts` — live Stripe pricing ($129/mo)
- `app/(site)/clasificados/comida-local/preview/ComidaLocalPreviewClient.tsx` — preview/checkout
- `app/lib/clasificados/comida-local/mapComidaLocalDraftToPreviewVm.ts` — draft → preview VM
- `app/lib/listingPlans/revenueStripe.ts` — Stripe Checkout Session creation
- `app/api/revenue-os/checkout/route.ts` — checkout API route

| ID | Requirement (short) | Status | Evidence file:line | Notes |
|---|---|---|---|---|
| C-001 | Comida Local visibly distinct from Restaurant | TRUE | `app/(site)/clasificados/comida-local/page.tsx:70-79` | Own branding/copy/color tokens (#7A1E2C); "Publicar tu puesto" CTA, "sin perfil de restaurante" tagline. |
| C-002 | No "Publicar restaurante" leakage | TRUE | `app/(site)/publicar/comida-local/ComidaLocalApplicationClient.tsx` (grep clean) | Case-insensitive grep for restaurant/Restaurante in comida-local files: zero matches. |
| C-003 | Current price is $129/month | TRUE | `app/lib/listingPlans/revenuePricingMatrix.ts:286-290` | `packageKey:"comida_local_base_monthly"`, `priceCents:12900`, `stripeEligible:true`. Surfaced at `comidaLocalFieldCopy.ts:36` and `checkpoint/page.tsx:19`. |
| C-004 | No current $99 path reachable | TRUE | `app/lib/clasificados/comida-local/comidaLocalPackages.ts:24-28` vs `revenuePricingMatrix.ts:283-284` | Legacy `COMIDA_LOCAL_BASIC_PACKAGE` ($99) still defined; code comment confirms "never wired to Stripe... superseded." Only consumer is read-only dashboard/admin label text (`mapComidaLocalDashboardListing.ts:69-70`) for **existing** rows, not a live purchase path. No new-purchase path exists → requirement satisfied, but dead/stale $99 code remains in the repo. |
| C-005 | No current $199 path reachable | TRUE | repo-wide grep, no `19900`/`$199` in comida-local | Not present anywhere in the category's code. |
| C-006 | No current $399 path reachable | TRUE | `revenuePricingMatrix.ts:201-205` (restaurantes_base_monthly only) | $399 is exclusively the Restaurantes package; comida-local checkpoint page explicitly notes Restaurantes "remains its own $399/mo product, untouched here" (`checkpoint/page.tsx:6-10`). |
| C-007 | No current $149 path reachable | TRUE | `comidaLocalPackages.ts:45-49` | Same as C-004: `COMIDA_LOCAL_PLUS_PACKAGE` ($149) is legacy/inert, only used for historical-row display labels, never bound to checkout. |
| C-008 | Food truck seller type | TRUE | `comidaLocalConstants.ts:52` | `value:"food_truck"`. |
| C-009 | Cart/puesto seller type | TRUE | `comidaLocalConstants.ts:53` | `value:"puesto"`, "Puesto / carrito". |
| C-010 | Home kitchen / comida desde casa | TRUE | `comidaLocalConstants.ts:54` | `value:"comida_casa"`. |
| C-011 | Pop-up seller type | TRUE | `comidaLocalConstants.ts:55` | `value:"pop_up"`. |
| C-012 | Feria/event vendor | TRUE | `comidaLocalConstants.ts:56` | `value:"feria"`, "Feria / mercado". |
| C-013 | Local catering seller type | TRUE | `comidaLocalConstants.ts:57` | `value:"catering"`. |
| C-014 | Meal prep seller type | TRUE | `comidaLocalConstants.ts:58` | `value:"meal_prep"`. |
| C-015 | Bakery/desserts seller type | TRUE | `comidaLocalConstants.ts:59` | `value:"panaderia"`, "Panadería / repostería". |
| C-016 | Private/personal chef | TRUE | `comidaLocalConstants.ts:60` | `value:"chef_privado"`. |
| C-017 | Delivery-only kitchen | TRUE | `comidaLocalConstants.ts:61` | `value:"delivery_only"`, "Solo entrega". |
| C-018 | Farmers/market vendor | TRUE | `comidaLocalConstants.ts:62` | `value:"mercado"`, "Vendedor de mercado". |
| C-019 | Custom "Other" seller type | TRUE | `comidaLocalConstants.ts:63` | `value:"otro"`. |
| C-020 | Spanish labels read naturally | TRUE | `comidaLocalConstants.ts:52-63` | All 12 labelEs values are natural colloquial Spanish, not literal machine translation. |
| C-021 | English labels are real English | TRUE | `comidaLocalConstants.ts:52-63` | All 12 labelEn values are natural English phrasing (e.g. "Stand / cart", "Bakery / pastry"). |
| C-022 | Stored seller-type IDs language-neutral | TRUE | `comidaLocalTypes.ts:41-53`, `comidaLocalConstants.ts:41-46` | `value` union never changes with `lang`; `comidaLocalOptionLabel()` only switches the displayed label. Note: some key strings are Spanish-origin words ("puesto","feria","panaderia","mercado") used as opaque IDs, but they are stable regardless of UI language. |
| C-023 | Custom seller-type "Other" supports multi-word/spacebar/Add/remove/persistence | FALSE | `ComidaLocalApplicationClient.tsx:621-630`; `comidaLocalTypes.ts:138` | Plain single `<input>` bound to scalar `businessTypeCustom: string`; no keydown/space-blocking (multi-word typing structurally unblocked, but the actual keystroke behavior is RUNTIME-REQUIRED to fully confirm). **No Add button, no remove/chip-list UI exists** — this is a single free-text field, not a repeatable list. Persistence: TRUE (`comidaLocalDraftPersistence.ts:231`, capped 80 chars). |
| C-024 | Food truck/mobile gets current/live-location controls | FALSE | `ComidaLocalApplicationClient.tsx:357-377`, `:841-868` | `locationNote`/`locationUrl` fields are shown unconditionally to **every** seller type; only a generic "mobile" banner (line 842-850, shared by food_truck/puesto/mercado/delivery_only/pop_up/feria) differs. Not an exclusive/tailored control for mobile sellers — same UI, cosmetic banner only. |
| C-025 | Food truck/mobile gets service area control | FALSE | `ComidaLocalApplicationClient.tsx:635-677` | `zoneNote`/`cityDisplay` ("Zona" section) identical for all 12 seller types; no mobile-specific service-area/radius control. |
| C-026 | Food truck/mobile gets schedule/hours control | FALSE | `ComidaLocalApplicationClient.tsx:877-898` | `weeklyHours`/`HoursEditor` shared identically across all seller types; no mobile-specific schedule variant. |
| C-027 | Food truck/mobile gets real order/contact link | FALSE | `ComidaLocalApplicationClient.tsx:777-828` | No dedicated "order link" field for mobile sellers — only the generic `additionalWebsites` repeatable list (shared by all types) and the generic `locationUrl`. |
| C-028 | Home kitchen gets private-by-default exact street address | TRUE | `createEmptyComidaLocalDraft.ts:30` (`showAddressPublicly: false`); `ComidaLocalApplicationClient.tsx:932-967` | Address field defaults private for every seller type (mechanism shared, not exclusive), and a home-kitchen-specific banner (line 932-940) reinforces the privacy default. Outcome is satisfied for home-kitchen sellers even though the mechanism is generic. |
| C-029 | Home kitchen can show service city/area | TRUE | `ComidaLocalApplicationClient.tsx:635-677` | `cityDisplay`/`zoneNote` ("Zona" section) always available, usable by home-kitchen sellers. |
| C-030 | Home kitchen supports pickup/delivery | TRUE | `comidaLocalConstants.ts:70-71` (`pickup`,`delivery` service options) | Selectable by any seller type including home kitchen via shared `serviceOptions` chips. |
| C-031 | Home kitchen private fulfillment guidance doesn't leak publicly | TRUE | `mapComidaLocalDraftToPreviewVm.ts:272` (`businessAddressLine = draft.showAddressPublicly ? ... : ""`) | Address omitted entirely from the public/preview VM unless explicitly toggled on; the home-kitchen banner is editor-only guidance, never rendered publicly. |
| C-032 | Pop-up/feria gets event/location schedule | FALSE | `ComidaLocalApplicationClient.tsx:357-365`, `:842-850` | `pop_up`/`feria` are bucketed into the same "mobile" `sellerCategory` as food_truck/puesto/mercado/delivery_only and shown the identical "Como vendedor móvil" banner — no event-specific schedule field or copy. |
| C-033 | Pop-up/feria gets temporary-location link | FALSE | `ComidaLocalApplicationClient.tsx:860-868` | `locationUrl` is available (satisfies the underlying need) but is the same generic field shared by all types with the same mobile-generic banner — not pop-up/feria-specific. |
| C-034 | Catering gets quote/contact guidance | TRUE | `ComidaLocalApplicationClient.tsx:683-691` | Dedicated catering-only banner: "describe tamaños de evento, mínimos... Agrega tu formulario de cotización en «Enlaces adicionales»" — genuinely tailored copy distinct from other buckets. |
| C-035 | Catering gets service radius/area | FALSE | `ComidaLocalApplicationClient.tsx:635-677` | Same shared `zoneNote`/`cityDisplay` as every other seller type; no radius-specific control. |
| C-036 | Catering gets event information capture | FALSE | `ComidaLocalApplicationClient.tsx:701-709` | Only the shared freeform `queVendes` textarea plus the banner nudging users to describe events there — no dedicated structured event-info field. |
| C-037 | Meal prep gets recurring pickup/delivery schedule | FALSE | `ComidaLocalApplicationClient.tsx:692-699`, `:877-898` | Banner nudges toward describing weekly menu, but `weeklyHours`/`serviceOptions` are the same shared generic controls used by every seller type — no meal-prep-specific recurring-schedule UI. |
| C-038 | Meal prep gets order URL | FALSE | `ComidaLocalApplicationClient.tsx:692-699`, `:777-828` | Banner tells sellers to "Agrega tu enlace de pedidos en «Enlaces adicionales»" — same generic `additionalWebsites` mechanism used by catering and every other type, not a dedicated order-URL field. |
| C-039 | Switching seller type doesn't corrupt unrelated saved data | TRUE | `ComidaLocalApplicationClient.tsx:605-610` | `onChange` for `businessType` only conditionally clears `businessTypeCustom` when the new value isn't `"otro"`; no other draft field is touched by a businessType change. |
| C-040 | Pickup service mode | TRUE | `comidaLocalConstants.ts:70` | `value:"pickup"`. |
| C-041 | Delivery service mode | TRUE | `comidaLocalConstants.ts:71` | `value:"delivery"`. |
| C-042 | Preorder service mode | TRUE | `comidaLocalConstants.ts:73` | `value:"preorder"`. |
| C-043 | Scheduled pickup | TRUE | `comidaLocalConstants.ts:74` | `value:"scheduled_pickup"`. |
| C-044 | Custom order | TRUE | `comidaLocalConstants.ts:75` | `value:"custom_order"`. |
| C-045 | Catering service mode | TRUE | `comidaLocalConstants.ts:76` | `value:"catering"`. |
| C-046 | Events/ferias service mode | TRUE | `comidaLocalConstants.ts:77` | `value:"events"`, "Eventos". |
| C-047 | Mobile service mode | TRUE | `comidaLocalConstants.ts:78` | `value:"mobile"`. |
| C-048 | Market pickup | TRUE | `comidaLocalConstants.ts:79` | `value:"market_pickup"`. |
| C-049 | Meal prep service mode | TRUE | `comidaLocalConstants.ts:80` | `value:"meal_prep"`. |
| C-050 | Limited daily quantity | TRUE | `comidaLocalConstants.ts:81` | `value:"limited_daily_quantity"`. |
| C-051 | "Other" custom service mode | TRUE | `comidaLocalConstants.ts:82` | `value:"other"`. |
| C-052 | Service modes tailored, not copy-pasted from Restaurant | TRUE | `restauranteTaxonomy.ts:94-105` (Restaurant reference) vs `comidaLocalConstants.ts:70-83` | Restaurant-only concepts (`dine_in`,`takeout`,`personal_chef`) absent from Comida Local's list; Comida Local adds concepts absent from Restaurant (`scheduled_pickup`,`market_pickup`,`limited_daily_quantity`,`preorder`,`custom_order`) — evidence of genuine tailoring. |
| C-053 | Custom "Other" service mode supports multi-word/spacebar/Add/remove/persistence | FALSE | `ComidaLocalApplicationClient.tsx:922-931` | Plain `<input>` bound to scalar `serviceOptionOtherCustom: string`; no keydown blocking (multi-word/spacebar structurally unblocked, live typing is RUNTIME-REQUIRED). **No Add button, no remove/chip-list** — single free-text field only. Persistence TRUE. |
| C-054 | Multiple service modes coexist | TRUE | `ComidaLocalApplicationClient.tsx:114-116`, `:900-921` | `toggleInList()` on an array (`serviceOptions: ComidaLocalServiceOption[]`), no mutual-exclusivity logic. |
| C-055 | Homemade / hecho en casa highlight | TRUE | `comidaLocalConstants.ts:89` | `value:"hecho_en_casa"`, "Homemade". |
| C-056 | "Fresh daily" highlight | FALSE | `comidaLocalConstants.ts:86-106` (full list) | Only generic "ingredientes_frescos"/"Fresh ingredients" exists (`:91`); no daily-freshness-specific option. |
| C-057 | Made to order highlight | TRUE | `comidaLocalConstants.ts:97` | `value:"hecho_al_momento"`, "Made to order". |
| C-058 | Family owned highlight | TRUE | `comidaLocalConstants.ts:103` | `value:"familiar"`, "Family-owned". |
| C-059 | Local ingredients highlight | FALSE | `comidaLocalConstants.ts:104` | Closest analog is `value:"local"` / "100% local" — a business-level claim, not specifically "local ingredients". |
| C-060 | Custom orders highlight | TRUE | `comidaLocalConstants.ts:100` | `value:"pedidos_personalizados"`, "Custom orders". |
| C-061 | Preorder highlight | FALSE | `comidaLocalConstants.ts:86-106` | "Preorder" exists only as a service option (`:73`), absent from the highlight registry. |
| C-062 | Pickup highlight | TRUE | `comidaLocalConstants.ts:102` | `value:"pickup_disponible"`, "Pickup available". |
| C-063 | Delivery highlight | TRUE | `comidaLocalConstants.ts:101` | `value:"entrega_disponible"`, "Delivery available". |
| C-064 | Weekend availability highlight | FALSE | `comidaLocalConstants.ts:86-106` | No such option exists in the highlight registry. |
| C-065 | Catering highlight | TRUE | `comidaLocalConstants.ts:99` | `value:"catering"`, "Catering available". |
| C-066 | Vegetarian/vegan/gluten-free (truthful, no fake certification) | FALSE | `comidaLocalConstants.ts:92-96` | Options exist (`vegetariano`,`vegano`,`sin_gluten`,`halal`,`kosher` — that part is TRUE). But no disclaimer/tooltip found anywhere (`comidaLocalFieldCopy.ts`, `ComidaLocalApplicationClient.tsx` grepped for "certif"/"self-report") clarifying these are self-declared rather than certified — the "no fake certification implied" safeguard the requirement asks for is absent, so the compound requirement fails on that sub-part (no false claim is currently made, but nothing prevents one either). |
| C-067 | No Restaurant-only concepts (full bar/upscale/reservations) leaking in | TRUE | `restauranteTaxonomy.ts:113-129`, `restauranteAmenitiesCatalog.ts:58` (reference) vs `comidaLocalConstants.ts:86-106` | Restaurant's `full_bar`/`upscale`/`romantic`/`reservations` values do not appear anywhere in Comida Local's highlight list. |
| C-068 | Custom "Other" highlight supports multi-word/spacebar/Add/remove/persistence | FALSE | `ComidaLocalApplicationClient.tsx:1092-1101` | Plain `<input>` bound to scalar `highlightsOtherCustom: string`; no keydown blocking. **No Add button, no remove/chip-list.** Persistence TRUE. |
| C-069 | Section-specific Other inputs (not one generic catch-all) | TRUE | `ComidaLocalApplicationClient.tsx:591-600,621-630,922-931,1000-1009,1031-1068,1092-1101` | 6 distinct, section-scoped custom fields (`foodTypeCustom`, `businessTypeCustom`, `serviceOptionOtherCustom`, `paymentOtherNote`, `customLanguages`, `highlightsOtherCustom`), each gated by its own section's selection state — no shared bottom catch-all. |
| C-070 | Added custom value appears within its own group | TRUE | `ComidaLocalApplicationClient.tsx:591-600` etc. | Each custom field renders inline immediately after its own chip/select group in the same section. |
| C-071 | Existing legacy custom values remain compatible | TRUE | `comidaLocalDraftPersistence.ts:226-260` | All custom fields loaded via tolerant `safeString()`/`safeCustomLanguages()` with no format assumptions — always were free-text strings, so no migration needed. |
| C-072 | Additional section-scoped Other examples confirm the pattern | TRUE | `ComidaLocalApplicationClient.tsx:1000-1009` (`paymentOtherNote`), `:1031-1068` (`customLanguages`) | Two more section-scoped custom fields beyond the three named, confirming pattern consistency across the whole form. |
| C-073 | "Encuéntrame Hoy"/"Find Me Today" exists as real optional feature | TRUE | `ComidaLocalApplicationClient.tsx:833-840`; `comidaLocalFieldCopy.ts:204-217` (`optional: true`) | Dedicated "ubicacion" section/tab; both `locationNote` and `locationUrl` marked optional. |
| C-074 | Destination can be Maps | TRUE | `comidaLocalFieldCopy.ts:216-217`; `comidaLocalFormatting.ts:80-90` | Helper text explicitly says "Pin de Maps..."; `locationUrl` accepts any http(s) URL (no domain restriction). |
| C-075 | Destination can be Instagram | TRUE | `comidaLocalFormatting.ts:80-90` | Same generic URL field — no domain restriction, any URL incl. Instagram accepted. |
| C-076 | Destination can be Facebook | TRUE | `comidaLocalFormatting.ts:80-90` | Same as above. |
| C-077 | Destination can be TikTok | TRUE | `comidaLocalFormatting.ts:80-90` | Same as above. |
| C-078 | Destination can be website/ordering-page/event-page/custom URL | TRUE | `comidaLocalFormatting.ts:80-90` | Field is a fully generic URL validator (`u.protocol === "http:" \|\| "https:"`), no destination-type restriction of any kind. |
| C-079 | Optional status text supports multi-word typing/spacebar | RUNTIME-REQUIRED | `ComidaLocalApplicationClient.tsx:851-859` | Plain `<textarea>`, no keydown/space-blocking found in source, but actual typing/spacebar behavior can only be confirmed live in a browser per evidence doctrine. |
| C-080 | Today/current location DISTINCT from permanent address AND service area | TRUE | `comidaLocalTypes.ts:139-164` (comments at :153,:162) | `locationNote`/`locationUrl` (Find Me Today) vs `businessAddressLine`/`showAddressPublicly` (permanent address) vs `cityCanonical`/`cityDisplay`/`zoneNote` (service area) are three distinct field groups with explicit code comments confirming separation. |
| C-081 | Changing today location does NOT overwrite permanent address or service area | TRUE | `ComidaLocalApplicationClient.tsx:855,864` | `onChange` handlers for `locationNote`/`locationUrl` only ever call `updateDraft({ locationNote/... })`; no side effect touches `businessAddressLine`/`zoneNote`. |
| C-082 | Updating Find Me Today does not create new listing or trigger Stripe | TRUE | `ComidaLocalApplicationClient.tsx:386-432` (edit-save `handlePublish`); `:1277-1284` (new-listing gate) | Edit-mode save (Gate D19 comment, lines 181-184) updates the same row with no Stripe call; a brand-new listing's only publish path is Preview→Checkout regardless of which fields changed — updating Find Me Today alone never uniquely triggers a new listing/charge. |
| C-083 | Missing destination hides the CTA | TRUE | `mapComidaLocalDraftToPreviewVm.ts:215-223` | `const loc = normalizeLocationHref(draft.locationUrl); if (loc) { actions.push(...) }` — the "Dónde está hoy" contact action is only added when a valid URL exists. |
| C-084 | Private home address does not leak through directions | TRUE | `mapComidaLocalDraftToPreviewVm.ts:147-226,272` | The "location" contact action is built exclusively from `draft.locationUrl`, never from `businessAddressLine`; and `businessAddressLine` itself is blanked out of the preview VM entirely unless `showAddressPublicly` is true (line 272). |
| C-085 | Permanent address / service area / today-location are 3 separate canonical fields | TRUE | `comidaLocalTypes.ts:139-164` | Same evidence as C-080 — three distinct, non-aliased field groups. |
| C-086 | Real street-verification provider required before "Verified Address" claim | FALSE | `app/lib/businessAddress/businessAddressProvider.ts:4-19`; `comidaLocalAddressMappingExample.ts:34` | `manualOnlyAddressProvider` always returns `{ok:false, reason:"no_provider_configured"}` — no Google Places/Smarty/USPS integration exists anywhere in the repo. No "verified" badge exists either (repo-wide grep for "verified"/"verificad" in comida-local: zero matches), so no false claim is made, but the capability itself is absent/deferred. |
| C-087 | Phone field uses familiar formatting | TRUE | `app/components/forms/PhoneInput.tsx:36-63`; `ComidaLocalApplicationClient.tsx:718-726` | Auto-formats to `(xxx) xxx-xxxx` as you type via `formatUsPhone`. |
| C-088 | WhatsApp field is international-safe | TRUE (with caveat) | `comidaLocalFormatting.ts:45-58` (href builder strips non-digits, no length cap); `app/(site)/clasificados/en-venta/shared/utils/enVentaPhoneDisplay.ts:22-34` (formatter) | For >11 digits or 11-digit numbers not starting with "1", raw input (incl. "+"/country code) is preserved unmutated. Caveat: exactly-10-digit numbers are still force-formatted as US pattern regardless of true origin. |
| C-089 | Email field is real and persists | TRUE | `ComidaLocalApplicationClient.tsx:739-749`; `comidaLocalDraftPersistence.ts:241` (`safeString(parsed.email, 254)`) | Real controlled `<input type="email">`, persists via localStorage draft and round-trips through edit hydration. |
| C-090 | "Correo" uses rich shared modal only with real email | TRUE | `ComidaLocalContactActions.tsx:49-69`; `mapComidaLocalDraftToPreviewVm.ts:164-172` | `CtaActionSheet` (shared component) opens only for the `"email"` action, which is only added to `contactActions` when `draft.email.trim()` is non-empty. |
| C-091 | Additional websites repeatable | TRUE | `ComidaLocalApplicationClient.tsx:777-828` | "+ Agregar enlace" button appends `{label:"",url:""}`, capped at 6. |
| C-092 | Each has Title + URL | TRUE | `ComidaLocalApplicationClient.tsx:781-800` | One label input + one URL input per row. |
| C-093 | Multiple entries coexist | TRUE | `comidaLocalTypes.ts:181-184` (`ComidaLocalAdditionalWebsite[]`) | Stored as an array, not a single overwritable slot. |
| C-094 | Additional websites survive refresh | TRUE (persistence wiring) | `comidaLocalDraftPersistence.ts:149-161` (`safeAdditionalWebsites`, cap 6) | Included in `saveComidaLocalDraftToStorage`/`loadComidaLocalDraftFromStorage` round trip via localStorage. Actual browser-refresh behavior is RUNTIME-REQUIRED for full confirmation. |
| C-095 | Additional websites survive Preview→Edit | TRUE | `mapComidaLocalDraftToPreviewVm.ts:109-118,271`; `comidaLocalListingEditContext.ts:113` | Forward: included in preview VM. Reverse: edit draft hydrates from the same stored `listing_json`/localStorage draft (not a separately-persisted VM), so the round trip is wired end to end. |
| C-096 | Social links truthful (no fake "verified" claims) | TRUE | `comidaLocalFormatting.ts:95-126` | Plain URL/handle normalization only; repo-wide grep for "verified"/"verificad" in comida-local: zero matches. |
| C-097 | Language input follows shared standard pattern | TRUE | `app/components/forms/LanguagesInput.tsx:3-12` (comment: "generalized from Restaurantes' working language-chip implementation") | Shared component wired identically at `ComidaLocalApplicationClient.tsx:1031-1068`. |
| C-098 | English shell validation messages fully English | TRUE | `comidaLocalValidation.ts:54,62,70,98,106,128,137-138,146,156-157` | Every issue message uses an explicit `es ? "..." : "..."` ternary; no Spanish-only string found. **Separate note:** `ComidaLocalApplicationClient.tsx:403` hardcodes `lang: "es"` literally (not the `es` boolean variable) in the edit-save `postComidaLocalPublishApi` call — a real hardcoded-locale defect, though it affects the save-API call, not the validation-message UI covered by this requirement. |
| C-099 | English shell field labels/helpers/placeholders in English | TRUE | `comidaLocalFieldCopy.ts:7-15` (type requires both `labelEs`/`labelEn`, comment: "never a Spanish-only fallback presented as English") | Every field entry has both variants explicitly authored. |
| C-100 | English shell sidebar/nav in English | TRUE | `ComidaLocalValidationPanel.tsx:53,69`; `comidaLocalFieldCopy.ts:30-79` (`COMIDA_LOCAL_SHELL_COPY`) | Bilingual shell copy object, fully English when `es=false`. |
| C-101 | Spanish shell remains Spanish | TRUE | `ComidaLocalApplicationClient.tsx` (spot-checked) | Virtually every string gated by `es ? ... : ...`; one deliberate exception is the shared word "Extras" (line 975, explicit comment that it's a real shared word, not leakage). |
| C-102 | Hours follow shared standard schema | TRUE | `app/lib/businessHours/computeBusinessHoursStatus.ts:1-8` (comment: "generalized from Restaurantes' working `computeHoursStatus`") | Comida Local imports this shared module directly (`comidaLocalTypes.ts:5`). |
| C-103 | Hours respect mobile/event seller realities | TRUE | `comidaLocalTypes.ts:156-159`; `ComidaLocalApplicationClient.tsx:869-898` | `availabilityNote` freeform text coexists alongside structured `weeklyHours`; explicit helper: "Leave it blank if your schedule changes constantly." |
| C-104 | Legacy availability data remains compatible | TRUE | `comidaLocalDraftPersistence.ts:136-147` (`safeWeeklyHours()` returns `{}` on non-record input) | A legacy draft with only `availabilityNote` and no `weeklyHours` merges cleanly with an empty object — no crash/data loss. |
| C-105 | Gallery/media upload behavior robust | TRUE | `comidaLocalImageValidation.ts` (12MB cap, MIME allowlist, unsafe-URL rejection, role/size checks) | Real validation logic, not just component presence. |
| C-106 | Empty videos array doesn't create empty Videos tab | TRUE (N/A) | `comidaLocalTypes.ts`, `createEmptyComidaLocalDraft.ts:4-43` | No video field exists in this category at all (repo-wide grep for "video" under comida-local: zero matches) — nothing to gate, trivially satisfied. |
| C-107 | Existing media survives hydration/hard refresh | TRUE (persistence wiring only) | `useComidaLocalDraft.ts:27-36`; `comidaLocalDraftPersistence.ts:262-267` | `mainPhoto`/`logoImage`/`galleryImages` restored via `normalizeComidaLocalImageFromStorage`/`normalizeComidaLocalImageListFromStorage` on mount. Actual hard-refresh behavior in a live browser is RUNTIME-REQUIRED. |
| C-108 | Preview reads active locale | TRUE | `ComidaLocalPreviewClient.tsx:46-47` (`const routeLang = normalizeLang(searchParams?.get("lang")); const es = routeLang !== "en";`) | Reads `lang` from the URL search param, same pattern as the application form. |
| C-109 | Preview→Edit preserves locale | TRUE | `ComidaLocalPreviewClient.tsx:79-81` (`backToEditHref` includes `&lang=${routeLang}`) | Edit link explicitly carries the current locale forward. |
| C-110 | Preview→Checkout preserves locale | TRUE | `ComidaLocalPreviewClient.tsx:177-183` (`locale: es ? "es" : "en"` passed into `startRevenueCategoryCheckout`) | Explicitly derived from the live `es` variable, not a hardcoded literal. |
| C-111 | Checkout locale NOT hardcoded to Spanish | TRUE | `ComidaLocalPreviewClient.tsx:181`; `revenueCategoryCheckoutClient.ts:31`; `app/api/revenue-os/checkout/route.ts:503,749,757` | Full chain traced: client passes live `es`-derived locale → `startRevenueCategoryCheckout` forwards `input.locale` → server route reads `body.locale` (defaults to "es" **only when absent**, not force-overridden) → used for success/cancel URL localization. No hardcoded `"es"` literal found in this specific Preview→Checkout path. **Caveat:** `revenueStripe.ts`'s `sessionParams` (lines 162-183) never sets Stripe's own `locale` field at all — the Stripe-hosted Checkout page itself falls back to Stripe's own auto-detection, not to a Leonix-hardcoded value, so this is a gap (Stripe UI language isn't explicitly forced to match the app's locale) rather than the specific "hardcoded to Spanish" bug described. |
| C-112 | Final price remains $129/month | TRUE | `revenuePricingMatrix.ts:290` (`priceCents: 12900`) | Same package (`comida_local_base_monthly`) used for both display and Stripe line-item amount. |
| C-113 | Existing filled Comida Local ad requires no refill | TRUE | `ComidaLocalApplicationClient.tsx:251-270` (edit hydration sets full `draft` from DB row); `mapComidaLocalPublicListing.ts:130-158,160-168` | Edit flow hydrates every owner-fillable field from the row's `listing_json` (comment: "every field the owner can fill in... is only ever stored in listing_json"), not a narrow subset. |

## Summary counts (113 total)

- TRUE: 93 (89 unqualified + 4 marked "TRUE (...)" with an explicit caveat in the Notes column: C-004, C-028, C-088, C-094/C-107-style persistence-only caveats)
- FALSE: 19
- RUNTIME-REQUIRED: 1 (C-079; two additional rows — C-094, C-107 — are TRUE on the persistence-code path but explicitly flag that full behavioral confirmation of "survives refresh" is RUNTIME-REQUIRED)

Note: several TRUE verdicts above represent "capability exists generically, shared across all seller
types" rather than "seller-type-exclusive tailored UI" — see the C-024–C-039 block, which is the
most significant systemic gap found in this audit.
