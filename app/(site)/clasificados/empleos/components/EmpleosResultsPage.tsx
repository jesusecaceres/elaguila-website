"use client";

import { EmpleosResultsView } from "./EmpleosResultsView";

/** Results browse layer for `/clasificados/empleos/resultados` (search + filters + sample listings). */
export function EmpleosResultsPage() {
  return <EmpleosResultsView />;
}
