/**
 * Reservado: mapeo formulario Privado → vista previa (mismo patrón que `brNegocioInputToPreviewMap.ts`).
 * Vista previa en vivo: `mapBienesRaicesPrivadoStateToPreviewVm` + borrador local. La plantilla demo sigue en `buildBienesRaicesPrivadoTemplateVm` (preview/privado/model) si hace falta.
 */

export const PRIVADO_PREVIEW_MAP_STUB_VERSION = 1;
