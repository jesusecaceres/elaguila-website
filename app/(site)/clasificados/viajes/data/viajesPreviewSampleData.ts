import { VIAJES_OFFER_DETAILS } from "./viajesOfferDetailSampleData";

/** Draft-style preview payload — mirrors a published offer detail. */
export const VIAJES_PREVIEW_OFFER = VIAJES_OFFER_DETAILS["cancun-resort-mar"]!;
