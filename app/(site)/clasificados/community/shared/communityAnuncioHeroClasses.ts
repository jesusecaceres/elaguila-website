/** Shared hero frame for Clases/Comunidad public detail (flyer-first). */
export const COMMUNITY_ANUNCIO_HERO_FRAME =
  "relative overflow-hidden rounded-xl border border-[#C9B46A]/40 bg-[#F4F0E6] flex items-center justify-center mb-6 min-h-[min(52vh,480px)] max-h-[min(88vh,900px)] px-2 py-4 sm:px-4";

/** Quick preview card hero — matches public flyer-first sizing (no outer mb; card handles rhythm). */
export const COMMUNITY_PREVIEW_HERO_FRAME =
  "relative w-full overflow-hidden bg-[#F4F0E6] flex items-center justify-center min-h-[min(52vh,480px)] max-h-[min(88vh,900px)] px-2 py-4 sm:px-4";
