/**
 * Shared layout tokens for the public Autos landing (alignment with En Venta / Viajes category presence).
 * Widen slightly on 2xl so large desktops do not feel like a narrow column.
 */
export const autosLandingSectionClass =
  "mx-auto w-full min-w-0 max-w-[min(100%,1280px)] 2xl:max-w-[1360px] px-4 sm:px-5 md:px-6 lg:px-8 2xl:px-12";
