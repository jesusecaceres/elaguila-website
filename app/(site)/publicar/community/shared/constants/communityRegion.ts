/**
 * Community quick (Clases + Comunidad) discovery region.
 *
 * The internal/discovery region is **always NorCal** so listings in any NorCal city
 * are findable across the whole region. The public city remains the *real* city
 * where the class is offered or the event is happening — never replaced by NorCal
 * on cards/details/previews.
 */
export const COMMUNITY_DISCOVERY_REGION = "NorCal";

/** Public location is always set explicitly by the user; default state for forms. */
export const COMMUNITY_DEFAULT_STATE = "CA";
