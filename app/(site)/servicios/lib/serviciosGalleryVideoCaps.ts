/** Public Servicios gallery — matches application `SERVICIOS_MAX_VIDEO_URLS` (8).
 * Global Business Hub OS — pilot-lane video cap raised 4 -> 8, moved in lockstep with the
 * application-side constant. */
export const MAX_SERVICIOS_PUBLIC_GALLERY_VIDEOS = 8;
