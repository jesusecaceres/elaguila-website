"use client";

import PublicarCategoryApplication from "@/app/clasificados/publicar/PublicarCategoryApplication";

/** Restaurantes–owned publish entry for `/clasificados/publicar/restaurantes` (wizard body still shared until split). */
export default function RestaurantesPublicarPage() {
  return <PublicarCategoryApplication />;
}
