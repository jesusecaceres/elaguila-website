"use client";

import PublicarCategoryApplication from "@/app/clasificados/lib/PublicarCategoryApplication";

/** Autos–owned entry for `/clasificados/publicar/autos` (dispatched from shared route). */
export default function AutosPublicarPage() {
  return <PublicarCategoryApplication />;
}
