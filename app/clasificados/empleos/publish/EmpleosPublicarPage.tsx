"use client";

import PublicarCategoryApplication from "@/app/clasificados/publicar/PublicarCategoryApplication";

/** Empleos–owned publish entry for `/clasificados/publicar/empleos` (wizard body still shared until split). */
export default function EmpleosPublicarPage() {
  return <PublicarCategoryApplication />;
}
