import type { RentasNegocioSocialLink } from "@/app/clasificados/rentas/listing/types/rentasAnuncioLiveTypes";

/** Stub — extend when BR negocio social parsing is wired end-to-end. */
export function parseBrNegocioRedesSocialLinks(_raw: string): RentasNegocioSocialLink[] | null {
  return null;
}
