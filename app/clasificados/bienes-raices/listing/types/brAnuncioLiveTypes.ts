/** Minimal shapes for BR live anuncio hooks — extend when the full BR pipeline is restored. */

export type BrAnuncioListingLike = Record<string, unknown>;

export type BrSameCompanySampleItem = { id: string; category?: string };
