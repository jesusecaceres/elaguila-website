"use client";

import PublicarCategoryApplication from "@/app/clasificados/publicar/PublicarCategoryApplication";

/** Travel–owned publish entry for `/clasificados/publicar/travel` (wizard body still shared until split). */
export default function TravelPublicarPage() {
  return <PublicarCategoryApplication />;
}
