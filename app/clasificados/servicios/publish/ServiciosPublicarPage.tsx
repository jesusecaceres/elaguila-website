"use client";

import PublicarCategoryApplication from "@/app/clasificados/lib/PublicarCategoryApplication";

/** Servicios–owned publish entry for `/clasificados/publicar/servicios` (wizard body still shared until split). */
export default function ServiciosPublicarPage() {
  return <PublicarCategoryApplication />;
}
