/**
 * Reservado: estado del formulario Bienes Raíces — Privado (paridad con Negocio en siguiente entrega).
 * No usar en producción hasta que exista el flujo completo.
 */
export type BienesRaicesPrivadoFormStub = Record<string, never>;
