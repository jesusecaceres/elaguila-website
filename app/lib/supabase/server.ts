// app/lib/supabase/server.ts
// Server-side helper (placeholder for future phases).
// We are keeping Batch 1 fully client-safe to avoid dependency assumptions.
//
// In later phases we can move protected routes + callback exchange server-side
// using Supabase SSR helpers if/when they are added to the repo.

export {};
