/** Per-field copy merged from code defaults + `clasificados_category_content` (admin). */
export type EnVentaDetailFieldUi = {
  label: string;
  placeholder?: string;
  help?: string;
};

export type EnVentaDetailFieldCopyMap = Record<string, EnVentaDetailFieldUi>;
