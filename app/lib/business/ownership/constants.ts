export const OWNERSHIP_CLAIM_FLAG_KEY = "business_ownership_claim";

/** Matches the migration's default `expires_at = now() + interval '14 days'`. */
export const OWNERSHIP_CLAIM_DEFAULT_TTL_DAYS = 14;
